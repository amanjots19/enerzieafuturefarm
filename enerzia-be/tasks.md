# Enerzia backend — tasks

Agile backlog. One task at a time, finished completely (code + tests + lint +
docs) before the next is started. See `../.claude/skills/enerzia-backend/SKILL.md`
for the definition of done.

Status: `TODO` · `WIP` · `DONE` · `BLOCKED`

---

## In progress

**Phase 11 (order book & shipping labels)** — contract in `roadmap.md` §Admin
orders and `schema.md` §orders, landed 2026-08-17. **11.1 and 11.2 DONE.**

**11.1, 11.2, 11.2-verify, 11.10, 11.3, 11.3b, 11.7, 11.4 and 11.5 DONE.** The
order book API is complete except the label.

**Phase 11 backend is COMPLETE** — 11.1, 11.2, 11.3, 11.3b, 11.4, 11.5, 11.6,
11.10 all done, plus the console (11.7, 11.7b). The shipping origin is
configured and a real 4×6 label prints from real data.

Next: a **print button** on the order detail screen — the label endpoint has no
UI entry point yet, the same gap 11.7b just closed for 11.4/11.5.

Also open: **11.11** (placed orders in Atlas carry no payment method — decide
before launch), **11.7-touch** / **11.2-touch** (both pre-existing), **11.9**
(remove the dead `Status` values).

Also open: **11.11** (placed orders in Atlas carry no payment method — decide
before launch), **11.7-touch** and **11.2-touch** (both pre-existing),
**11.9** (remove the dead `Status` values).

Phases 0–6 complete. Phase 7 still needs re-planning: the two rows below
understate it badly.

**The workspace is now one git repo**, pushed to
`github.com/amanjots19/enerzieafuturefarm`. Both prior histories were preserved
via subtree import rather than discarded, so the static-site and backend
commits are still in `git log`. `Enerzia/` and `enerzia-be/` are ordinary
directories now, not nested repos.

Phase 6 gate: **CI green on GitHub** (run 31211813050) — lint, race tests and
the 90% coverage gate all pass in a runner with **no database and no secrets**,
which is independent proof that `mongotest` makes the suite genuinely hermetic.

## Blocked

_nothing blocking._ Razorpay **test** keys and the webhook secret are in `.env`
and working — the full journey was smoke-tested against live Atlas and their
real test API (see the Phase 5 gate).

One deferred item, not a blocker: Razorpay's dashboard rejects tunnel hostnames
(`.loca.lt` confirmed blocked), so a webhook delivered by Razorpay themselves
has never been received. Our own signed deliveries exercise the same handler
with the same secret. Resolve when the service is deployed somewhere with a
real domain; try `trycloudflare.com` or ngrok first if a tunnel is wanted
sooner.

Live keys are **not** in `.env` and should never be pasted into a chat — put
them straight into the deployed environment.

---

## Phase 0 — Foundation

| id | task | status | notes |
|---|---|---|---|
| 0.1 | Go module, directory layout, Makefile, `.golangci.yml`, `.env.example`, `.gitignore` | DONE | `make lint` / `test` / `cover` / `check`; 90% gate over `./internal/...` |
| 0.2 | Typed config loaded once from env, with validation and defaults | DONE | 100% cov; reports every problem at once; never echoes secret values |
| 0.3 | MongoDB connection: connect, ping, timeouts, clean disconnect | DONE | 95.8% cov via the `mongotest` fake — no live cluster needed. Remaining line is an unreachable defensive branch in `Disconnect`. |
| 0.4 | Router, JSON envelope helpers, error codes, 404/405 handlers, recovery + request-id + logging middleware | DONE | 100% cov; 404/405 go through the middleware chain too |
| 0.5 | `GET /health` incl. degraded state when Mongo is down | DONE | 100% cov; 503 + body when Mongo is down, driver detail logged not returned |
| 0.6 | `internal/mongotest` — in-process fake MongoDB (wire protocol handshake, commands, per-command failure injection) | DONE | Excluded from the coverage gate as test infrastructure; has its own suite. Replaces the `MONGO_TEST_URI` skips. |

Phase 0 gate at completion: **lint 0 issues · total coverage 99.2% · all tests green (race)**.

Phase 1 gate at completion: **lint 0 issues · total coverage 99.7% · all tests green (race)**.

Phase 3 gate at completion: **lint 0 issues · total coverage 97.4% · all tests green (race)** · full cart journey verified against live Atlas, and the stored document confirmed to contain no pricing.

Phase 2 gate at completion: **lint 0 issues · total coverage 98.2% · all tests green (race)** · full sign-in flow verified against live Atlas.

Phase 5 gate at completion: **lint 0 issues · total coverage 96.9% · all tests green (race)** · **full checkout journey verified against live Atlas and the real Razorpay test API** on 2026-08-08 — 18/18 checks. A real Razorpay order was created through their API; the signed `payment.captured` webhook moved the order to `placed` and emptied the cart; a replayed event id was deduped; a tampered body and a missing signature were both rejected 400; another shopper's read returned a 404 byte-identical to an unknown id. Server logs audited: no key, secret or Mongo URI present.

The one part still unproven: a webhook delivered **by Razorpay themselves**, which needs a public URL their dashboard accepts (`.loca.lt` is blocklisted). Our verification signs the payload with the real `RAZORPAY_WEBHOOK_SECRET`, so it exercises the true code path — but not their transport. Close this at deploy time.

## Model rework (user-requested, after Phase 4)

| id | task | status | notes |
|---|---|---|---|
| M.1 | ~~Variants become their own collection~~ | SUPERSEDED by M.5 | |
| M.5 | Every size becomes an independent product; `variants` collection folded away | DONE | 9 products, one per sellable item. Cart and orders hold a `productId`. `family` groups siblings for display only. Net simplification: one collection, one document per sellable thing. |
| M.2 | `stock` on the product; sold out derived, never stored | DONE | `TakeStock` guards with `$gte` **inside the filter**; verified on live Atlas that it refuses rather than going negative. `ReturnStock` compensates a partial failure. |
| M.3 | Addresses become an array on the user, each with its own `_id` | DONE | Multiple addresses, exactly one default, deleting the default promotes the next. `GET/POST /me/addresses`, `PUT/DELETE /me/addresses/{id}`. |
| M.4 | Contracts updated first: `schema.md`, `roadmap.md`, `product.md` | DONE | Now five modelling decisions; orders keep snapshotting deliberately. |

## Phase 1 — Catalogue

| id | task | status | notes |
|---|---|---|---|
| 1.1 | Product/variant domain model, `lineId` slug helper, discount calculation | DONE | `internal/catalogue`, 100% cov. Adds `Form`/`ParseFilter`, `Variant.DiscountPercent` verified against all 9 real prices, `Slug`/`LineID`/`SplitLineID`, and `Product.Validate` for seed and stored data. |
| 1.2 | Seed data for the 4 products + trust tiles + badges + nutrition, and a `seed` command | DONE | `internal/catalogue/seed.go` + `cmd/seed` (`make seed`), idempotent upserts. Tests pin all 9 prices to `product.md` §2. |
| 1.3 | Product repository against Mongo, tested with the `mongotest` fake | DONE | 100% cov. List/Get/TrustTiles/Seed/EnsureIndexes; tests assert the actual filter, sort and upsert flag sent to Mongo. |
| 1.7 | Smoke test the real Atlas connection once credentials land | DONE | Seeded 4 products + 4 tiles into `enerzia`, ran the API against it, exercised every endpoint incl. the full sign-in flow. Indexes verified on the live cluster. |
| 1.8 | `EnsureIndexes` per repository, called at startup | PARTIAL | Live cluster confirms `products.form_1`, `users.phone_1 UNIQUE`, `otp_codes.expiresAt_ttl` and `phone_1_createdAt_-1`. Orders indexes land with Phase 5. |
| 1.4 | `GET /api/v1/products` with `form` filter | DONE | 100% cov. Service parses the filter, handler maps `ErrUnknownForm` → 400. Detail content omitted from the list. |
| 1.5 | `GET /api/v1/products/{id}` with badges, nutrition, rating | DONE | 100% cov. Unknown id → 404; driver detail logged, never returned. |
| 1.6 | `GET /api/v1/content/trust` | DONE | 100% cov. Unseeded database returns `[]`, not an error — the strip is decoration. |

## Phase 2 — Auth

| id | task | status | notes |
|---|---|---|---|
| 2.1 | OTP model: hashed code, TTL index, attempt counter, single use | DONE | HMAC-keyed hash (not bare SHA-256 — see Discovered), single-use via a `consumed:false` filter, DB-side attempt counter |
| 2.2 | `POST /auth/otp/request`, `devCode` outside production | DONE | 202; rate limits checked before a code is generated so a flood costs one query, not a message |
| 2.3 | `POST /auth/otp/verify`, upsert user on first success | DONE | Closes the "any 6 digits" gap. One message for wrong/expired/spent/locked so nothing is enumerable. |
| 2.4 | JWT issue/verify + auth middleware | DONE | HS256 pinned; alg:none, foreign-key, wrong-issuer and no-expiry tokens all rejected |
| 2.5 | `GET /auth/me` | DONE | Stale token for a deleted account → 401, not 500 |

## Phase 3 — Cart

| id | task | status | notes |
|---|---|---|---|
| 3.1 | Cart model and line identity | DONE | Stored line is a pointer plus a count — no prices, per schema.md decision 1. Verified on disk. |
| 3.2 | Pricing/totals service — subtotal, MRP, savings, shipping threshold | DONE | Pure. Boundary tested at 49899/49900/49901 paise, plus a property check that the nudge and the charge can never disagree. |
| 3.3 | `GET /cart` | DONE | Empty cart is 200 with `lines: []`. Prices resolved live on every read. |
| 3.4 | `POST /cart/items` with increment-on-existing | DONE | Omitted qty defaults to 1; an explicit 0 is rejected. Validated against the catalogue before the cart is touched. |
| 3.5 | `PATCH /cart/items/{lineId}` — absolute qty, 0 deletes | DONE | A missing `qty` is 422, not a silent delete. |
| 3.6 | `DELETE /cart/items/{lineId}` and `DELETE /cart` | DONE | Unknown line → 404. |
| 3.7 | Delivery fee raised ₹49 → ₹50 (user, 2026-08-14) | DONE | **A price change, not new logic** — the below-threshold charge and the free-over-₹499 rule were already built in 3.2 and unchanged. Only `ShippingFee` moved, `4900 → 5000` paise, in `internal/cart/pricing.go`; it is the single source and every caller reads the computed result rather than re-deriving. `product.md` §4 updated in the same change so code and spec cannot disagree. `roadmap.md` needed nothing: it names the `shipping` field but its examples are all qualifying carts, so no fee value was pinned there. Three hardcoded literals also moved — `pricing_test.go` (one table row and the `TestPricingConstantsMatchProductMd` pin) and an `order/model_test.go` fixture; the pin deliberately keeps a literal rather than referencing the constant, or it would assert nothing. Boundary coverage unchanged and still exact: 0 / 1 / 49899 / **49900 free** / 49901 / 500000. `make check` green, `internal/cart` 95.5%, total 96.1%. **The running dev binary on :8080 predates this and will serve ₹49 until rebuilt.** |

## Phase 4 — Address

| id | task | status | notes |
|---|---|---|---|
| 4.1 | Address validator with the frontend's exact ordered messages | DONE | All five messages pinned verbatim, plus a walk test asserting the failure *order* matches the UI. Fields are trimmed on save. |
| 4.2 | `GET` / `PUT /me/address` | DONE | Absent address is `null`, not 404. A rejected write leaves the previous address untouched. No upsert — an address cannot conjure an account. |

## Phase 5 — Orders & payment

**Re-scoped 2026-08-07**: payment moves to **Razorpay** and **cash on delivery
is dropped** (user decision). `schema.md`, `roadmap.md` and `product.md` were
amended first — orders now exist in a `pending_payment` state holding a stock
reservation, and a signed callback or webhook is what makes them real. 5.1
predates the decision; 5.2 amends it.

| id | task | status | notes |
|---|---|---|---|
| 5.1 | Order model + `EFF-\d{6}` id generator | DONE | `internal/order`, 100% cov. `model.go` (`Order`, `Line`, `Totals`, `Payment`, `Status`) + `id.go` (`NewOrderID`/`ValidOrderID`). Snapshots decoupled from `cart` — own `Totals` type. `ShippingAddress` reuses `auth.Address`. Id is uniform over 000000–999999 via `crypto/rand` (no modulo bias, pinned by a distribution test), error-returning, stateless for the retry loop. |
| 5.2 | Amend the order model for Razorpay: new statuses, `payment` sub-document, drop COD | DONE | 100% cov. Added `StatusPendingPayment`/`StatusPaymentFailed`/`StatusExpired`; `PaymentStatus` type (created…refunded); `PaymentMethod` now reports Razorpay methods (upi/card/netbanking/wallet/emi/paylater); `PaymentCOD` deleted. `Payment` gains provider, status, amount, currency, three razorpay ids, method-detail fields (last4/network/vpa/bank/wallet), attempts, `*time.Time` capturedAt, and `*PaymentFailure` failure. `Order` gains `CreatedAt`/`ExpiresAt`; `PlacedAt` → `*time.Time`. `Validate` accepts a pending order (no method, nil PlacedAt) and rejects a placed order missing any of placedAt/method/capturedAt. Also added two cross-field checks in `Order.Validate`: (1) `Payment.Amount != Totals.Total` is rejected for every status — defence in depth per schema.md §Razorpay; (2) placed+ orders require `Payment.Status` to be `captured` or `refunded` — any pre-capture status (created/authorized/failed) is rejected. |
| 5.3 | Razorpay gateway behind an interface: create order, verify callback signature, verify webhook signature | DONE | `internal/razorpay`, 97.9% cov. `gateway.go` (Gateway interface, Client with 10s timeout, Unconfigured); `signature.go` (verifyCallbackSignature with keySecret, verifyWebhookSignature with webhookSecret). Both verifiers use `hmac.Equal`, never `==`. `NewClientWithBaseURL` allows tests to point at an httptest.Server — live API is never called from tests. `export_test.go` exposes `ClientBaseURL`/`ClientTimeout` for white-box assertions. `TestNewClient` pins the production base URL and 10s timeout. Config gains `RAZORPAY_KEY_ID`/`KEY_SECRET`/`WEBHOOK_SECRET` (required in production, optional elsewhere). Secret values never appear in errors. `TestCrossSecretRejection` proves each verifier rejects a valid signature made with the other secret. One line genuinely uncovered: the `json.Marshal` error branch in `CreateOrder` — unreachable because `CreateOrderRequest` contains only `int64` and `string` fields, which never fail to marshal. |
| 5.4 | Order repository: create, scoped reads, guarded status transition, indexes | DONE | `internal/order`, 100% cov. `repository.go` (Create, ByOrderID, ListForUser, MarkPlaced, EnsureIndexes). Two distinct sentinels for the two unique-index collisions on Create: `ErrDuplicateOrderID` (orderId_1) vs `ErrPendingOrderExists` (userId_1 partial) — classified by reading `keyPattern` from `WriteException.WriteErrors[0].Raw` (exact key lookup on "orderId"/"userId"), not by index-name substring. Tests use `mongotest.Reply` with `ok:1` + `writeErrors` + `keyPattern` so the driver produces a real `WriteException`. MarkPlaced takes `now time.Time` parameter (caller-supplied, deterministic in tests); tests assert `$set.updatedAt` equals the value passed, not a wall-clock instant. ByOrderID carries both orderId and userId in a single query. ListForUser excludes expired via `$ne`, sorts by createdAt desc. All 6 indexes with partial filter expressions per schema.md §Index creation. |
| 5.5 | `POST /orders`: ordered preconditions, expire any prior pending order, reserve stock, create the Razorpay order | DONE | `internal/order`: service.go + handler.go. Insert-first: order inserted without `razorpayOrderId` (omitempty avoids partial-index collision), then gateway.CreateOrder, then SetRazorpayOrderID. On any post-insert failure → releaseLines + ExpirePendingForUser → 502 GATEWAY_ERROR. ErrPendingOrderExists from Create returns reserved stock immediately → 409. Six-step precondition sequence enforced (empty-cart → blocking-line → address → expire-prior → stock → Razorpay). `Store` interface for fake injection in tests. `o.Validate()` called before repo.Create; failure is an internal 500 and releases stock. `Payment.Validate` relaxed for `PaymentStatusCreated` (empty RazorpayOrderID is valid before the gateway link). `internal/order`: 98.4% cov; total: 96.9%. |
| 5.6 | `POST /orders/{orderId}/payment/callback` | DONE | `ConfirmPayment` in service.go. Six-step guard: fetch → idempotent-placed-200 → not-pending-422 → signature-verify → razorpayOrderId-match → MarkPlaced. Webhook-beat-us (modified=false) re-fetches via ByOrderID. Cart cleared best-effort after successful transition. ErrSignatureInvalid used for all three verification failures so no security leak. Handler: empty body / missing fields → 422 field errors; ErrOrderNotFound → 404; ErrSignatureInvalid / ErrOrderNotPending → 422 "We could not verify that payment." `CartClearer` interface added; `*cart.Service` satisfies it in production. `providerRazorpay` const extracted (was at 4-occurrence threshold). `internal/server`: 100% — `TestAllHandlersAreRegistered` exercises auth, cart and order route registration. Unreachable dead code (not tested, by design): `userID !ok` body + the matching `return` in each handler (auth middleware always runs first); `genErr != nil` in the ID retry loop (analogous to json.Marshal in razorpay — crypto/rand.Read cannot fail in practice). |
| 5.7 | `POST /webhooks/razorpay`: raw-body HMAC, `payment_events` dedupe, capture/fail transitions | DONE | New files: `internal/order/events.go` (PaymentEvent, ErrDuplicateEvent, PaymentEventStore interface), `internal/order/events_repository.go` (PaymentEventsRepository.InsertEvent, IsDuplicateKeyError → ErrDuplicateEvent), `internal/order/webhook.go` (ErrWebhookSignatureInvalid, HandleWebhook, handleWebhookCapture, handleWebhookFailed). Modified: `service.go` (Store interface +ByRazorpayOrderID +MarkPaymentFailed; ServiceConfig/Service +Events; NewService), `repository.go` (+ByRazorpayOrderID +MarkPaymentFailed; bsonOpSet constant for goconst), `handler.go` (RegisterWebhook, razorpayWebhook — raw io.ReadAll before sig verify), `server/router.go` (RegisterWebhook on root r, outside /api/v1). Route: `POST /webhooks/razorpay` on root (no auth). Signature verified over raw bytes before any unmarshal (nolint:nilerr for deliberate swallow). Dedupe: insert-first (event id as _id). Amount assertion on capture. 200 for verified events including unknown/dup/order-not-found; 400 only for missing/invalid signature; 500 only on genuine DB failure. Stock released on payment.failed only when modified=true. Cart cleared best-effort using userID from resolved order. `make lint` clean. `internal/order`: 98.3%; total: 97.0%. |
| 5.8 | Reservation sweeper: expire abandoned pending orders and return their stock | DONE | New files: `internal/order/sweeper.go` (`SweepStore` interface, `Sweeper`, `NewSweeper`, `SweepOnce`, `Run`, `sweepReleaseLines`). Modified: `repository.go` (`FindExpiredPending` uses `status_1_expiresAt_1` index, `MarkExpired` guarded update); `config/config.go` (`SweeperInterval time.Duration`, default 1 min); `cmd/api/main.go` (goroutine: `go sweeper.Run(ctx, cfg.SweeperInterval)`). Write order is the entire point: guarded `MarkExpired` first, `ReturnStock` only when `modifiedCount == 1`. No TTL index. `Run` is tick-based, logs errors and continues so one bad sweep cannot kill the loop; stops cleanly on ctx cancel. Batch-bounded per tick (100). Tests: empty sweep; modifiedCount==0→no stock returned (the critical test); modifiedCount==1→all lines released; ReturnStock failure continues to next line; find error surfaces; mark error surfaces; Run stops on cancel; Run logs errors and continues. Repository tests assert the query filter and the update guard. `internal/order`: 98.1%; total: 97.0%. |
| 5.9 | `GET /orders` and `GET /orders/{orderId}` | DONE | Added `ListForUser` to `Store` interface; `ListOrders` and `GetOrder` service methods (thin pass-through, no business rules). New handlers: `listOrders`, `getOrder` in `handler.go`; new DTOs `listOrdersResponse`, `getOrderResponse`. Routes: `GET /api/v1/orders`, `GET /api/v1/orders/{orderId}`. Reuses existing `toPlacedOrderDTO`, `toOrderLineDTOs`, `toTotalsDTO` helpers. Pending orders: `placedAt`/`payment`/`etaText` absent via `omitempty` on nil pointers — never zero time or empty string. Other user's order: `ByOrderID` scopes by userId+orderId in one query; both cases return `ErrOrderNotFound`; handler returns identical 404 body. `TestOrderRouteRejectsWrongMethod` updated (GET is now registered; test uses PUT instead). Router test extended with GET /orders and GET /orders/{orderId} checks. `internal/order`: 97.9%; total: 96.9%. Phase 5 complete. **Note:** `ListForUser` filter was subsequently amended in 9.6 from `$ne expired` to `$in` over the five paid statuses. |

## Phase 6 — Hardening

| id | task | status | notes |
|---|---|---|---|
| 6.1 | OTP rate limiting (3 / 10 min, 1 / 30 s) | DONE | Landed with 2.2 rather than deferred; enforced before code generation |
| 6.2 | CORS from `ALLOWED_ORIGINS` | DONE | `internal/httpx/cors.go` (`CORS` middleware): exact-match allowlist, Vary: Origin always set when Origin present, wildcard never emitted, credentials not enabled. Preflight (OPTIONS + ACRM) returns 204 without reaching any handler. Disallowed origin: handler runs, no CORS headers. No-origin request: untouched. Wired as the outermost wrapper (`httpx.CORS(cfg.AllowedOrigins)(r)`) in `internal/server/router.go` so 404/405 fallback handlers also carry CORS headers. Config field already existed; not re-added. Unit tests in `internal/httpx/cors_test.go` (4 branch-covering tests + wildcard assertion); integration tests in `internal/server/cors_test.go` (7 tests through the real router). `internal/httpx`: 100%; `internal/server`: 100%; total: 97.0%. |
| 6.3 | Graceful shutdown with 15s drain | DONE | Landed with the Phase 0 wiring rather than deferred; the row was simply never updated. Verified live on 2026-08-08: SIGTERM logged `shutdown signal received, draining` with `grace=15s`, then `shutdown complete`, with `cfg.ShutdownGrace` bounding the drain in `cmd/api/main.go`. |
| 6.4 | CI workflow running lint + coverage gate | DONE | `.github/workflows/ci.yml`: triggers on push + PR; pins Go 1.25.5 (from go.mod) and golangci-lint v2.12.2 (matches local install); `actions/setup-go@v5 cache: true` caches module + build caches; installs golangci-lint to `$GOPATH/bin` so the Makefile's binary check passes; steps: Install golangci-lint → `make lint` → `make test` (race, separate step so data races are distinguishable from coverage failures) → `make cover` (90% gate enforced inside the target, not duplicated in workflow) → upload `coverage.out` artefact with `if: always()`. Suite verified offline with `env -i` stripping all secrets and MONGO_URI — every package passed cleanly. No Makefile changes needed. |

## Phase 7 — Frontend integration

Re-planned 2026-08-08 after exploring both sides. The old two rows badly understated
this: the storefront makes **zero** network calls today, so this is not wiring — three
screens are rebuilt. Decisions settled with the user: a thin review step (never a payment
page), full address management, JWT in `localStorage`, add-to-cart is an auth gate with
intent replay, payment resilience retries then reassures, thank-you is a screen.

Frontend files live in `Enerzia/`, not here.

| id | task | status | notes |
|---|---|---|---|
| 7.0 | `autoPort: false` in `Enerzia/.claude/launch.json` | DONE | It was `true` while `ALLOWED_ORIGINS` allows only `http://localhost:3000` — a busy port 3000 moved Next to 3001 and every call failed CORS with nothing logged server-side. |
| 7.1 | Typed API client under `lib/api/`, `.env.local`, `rupeeFromPaise` | TODO | 10 modules mirroring the backend packages. `request<T>()` throws rather than returning a union. DTO optionality must mirror the Go `omitempty` tags exactly. |
| 7.2 | The async seam: `app/shop/useShop.ts` | TODO | Reducer stays pure; the hook owns in-flight (ref, not state) and abort guards. Proven on the smallest slice: trust tiles from the server. |
| 7.4 | Catalogue 4 → 9 flat products | TODO | **Dispatched before 7.3** — `GET /products` is unauthenticated, so it has no dependency on auth, and it is what makes the shop visibly real. Atomic: `Variant`, `sel`, `pickVariant`, `variantOf`, `cartKey` all die. PDP family row comes from the loaded 9, **not** `detail.siblings` — which excludes the current product and has no `position`. |
| 7.3 | Auth + session restore | TODO | Real OTP, token persistence, boot sequence, global 401. A stale token signs out silently; a **network** error keeps the token. |
| 7.5 | Server-backed cart | TODO | Cart DTO replaces local state; totals rendered, never computed. Add-to-cart auth gate with `pendingIntent` replay. |
| 7.6 | Address list, add, select | DONE | Reuses the orphaned `.pay-*` CSS renamed to `.addr-*`. `validateAddress` stays as pre-flight — its messages are byte-identical to the server's. **Fix-up:** `AddressCard` had a button-in-button violation (`<button role="radio">` with `<button>Edit/Delete>` nested inside) causing React hydration errors. Restructured to `<div class="addr-opt">` + `<label class="addr-opt-main">` wrapping visually-hidden `<input type="radio">` + dot + text, with Edit/Delete as sibling `<span class="addr-actions">`. Dropped `role="radio"`, `aria-checked`, `stopPropagation`. Free keyboard nav via native radio group (ArrowUp/Down). CSS: removed button resets from `.addr-opt`, added `.addr-opt-main` and `.addr-radio` (clip/overflow hidden, 1px×1px, position:absolute). TypeScript clean. All 5 browser checks passed (Playwright/Chrome): no hydration errors, row selection works, Edit pre-fills form, Delete removes row, Tab reaches Edit button. |
| 7.7 | Review screen + `POST /orders` | TODO | `CheckoutScreen` → `ReviewScreen`; all payment UI deleted. Distinct handling for 409 empty/blocking, 409 pending, 422 no-address, 502 gateway. |
| 7.8 | Razorpay Checkout + thank-you screen | TODO | `settled` guard is mandatory — `ondismiss` can fire after a successful `handler`. Dismissal discards the orderId and re-posts; `OpenCheckout` step 4 makes that self-healing. |
| 7.9 | Dead-code sweep | TODO | `PAY_OPTIONS`, `PAY_LABELS`, `validatePayment`, `CardDetails`, `newOrderId`, local `totals`, `offLabel`, `FREE_SHIP_OVER`, `SHIP_FEE`, `PRODUCTS`, `TRUST`. |
## Two agents, one repo — ownership boundary (2026-08-12)

The **shop** (storefront + its API) and the **catalogue manager** (admin) are
being built in parallel by different sessions. Nothing enforces this, so it
only works if both sides read it.

| area | owner |
|---|---|
| `Enerzia/` storefront, `internal/{cart,order,msg91}`, shopper auth | shop |
| admin endpoints, `internal/catalogue` writes, image upload | catalogue |
| `internal/auth` | **shared — coordinate before deleting anything** |
| `internal/{httpx,server,config,mongodb}` | shared — additive changes only |

Known hazards:

- **`internal/auth` is the sharp one.** Task 8.2 deletes the OTP model,
  repository and `Sender`; admin login will add token issuing in the same
  package. 8.2 is **parked** until admin auth lands — the old endpoints are
  dormant, not harmful, and deleting shared foundations under someone else's
  feet is not worth the tidiness.
- **Contract files** (`roadmap.md`, `schema.md`, `product.md`, this file) are
  edited by both. Both sides have already written into `roadmap.md` §Auth. Keep
  to your own sections and re-read before editing rather than assuming the file
  is as you left it.
- **`products.images` is additive on the wire**, so the storefront ignores it
  safely today — it renders `grad` and will keep doing so until 9.5 below.

| id | task | status | notes |
|---|---|---|---|
| 9.5 | Storefront renders product photographs | DONE | `ProductDTO` gained `images: ImageDTO[]`; grid card and PDP hero render `images[0]`, falling back to `grad` when empty — still the common path (only `powder-100g` has a photo). No `next.config.ts` change: plain `<img>` was used rather than `next/image`, which avoids declaring the Cloudinary host in `remotePatterns` and makes the error fallback straightforward. **Scope:** grid + PDP only. Cart thumbs and order history deliberately excluded — `CartLineDTO` carries no image and adding one is a contract change the user deferred. **User-requested during the task:** the thumb strip shows exactly as many thumbnails as there are images (decorative `THUMBS` gradients only when there are none), the strip scrolls horizontally, and the mobile grid is two columns instead of one. **Two defects caught in verification, both in the multi-image path the reports could not exercise:** (1) `.pdp-thumb-btn--active` used `var(--brand)`, which is undefined — the whole `outline` shorthand was invalid, so the selected thumbnail had no indicator at all; now `var(--color-accent)`. (2) The `onError` fallback mutated the DOM imperatively (`style.display='none'` plus a background on the parent), which React never reset, so one failed image left the PDP permanently stuck on the gradient even after switching to a good photo; replaced with URL-keyed React state (`failedUrls`) that recovers. Files: `lib/api/types.ts`, `components/shop/ShopScreen.tsx`, `components/shop/PdpScreen.tsx`, `styles/shop.css`. TypeScript clean. Verified in a real browser against live data, including a broken-asset probe for the failure and recovery paths. |

## Phase 9 — Account area

**User decision, 2026-08-12.** With the purchase flow working end to end, the
shop needs somewhere for a signed-in shopper to live: order history, saved
addresses, contact, and sign out.

Placement: an account control immediately **left of the Cart pill**. Cart stays
the far-right anchor because it is the primary action; account sits beside it,
where people look for it. Signed out, that slot is a plain "Sign in" button —
which also gives sign-in an entry point that is not hidden behind clicking Cart.

**No backend work.** `GET /api/v1/orders` and `GET /api/v1/orders/{orderId}`
were built and verified in 5.9 and have been unused since — nothing in the
purchase flow consumed them. This phase is the consumer they were waiting for.

| id | task | status | notes |
|---|---|---|---|
| 9.1 | Header account control, dropdown menu, sign out | DONE | Accessible menu (`aria-expanded`/`aria-haspopup`, Escape closes and restores focus, click-outside, arrow-key navigation). Sign out clears token, cart, addresses, orders and the 7.12 selection buffer. **Superseded in part by 9.7:** the signed-out state no longer renders a plain "Sign in" text button — the person icon is always the trigger, and the signed-out dropdown holds a single "Sign in" item. Signed-out path verified in a browser (icon trigger, one menuitem, no phone header, `aria-expanded` correct); the signed-in menu still needs one real OTP sign-in to re-confirm after 9.7. |
| 9.2 | "My orders" screen — list with inline expand | DONE | `GET /orders`, newest first. Collapsed row: order id, date, total, status. Expanded: lines, quantities, prices, address, payment label, ETA. Two defects were caught in verification and fixed: the order id was truncated to `#F-118531` by a `shortId` helper, and a CSS gradient (`line.grad`) was interpolated into the line meta and printed as visible text — both hidden by a mocked fixture and exposed within seconds by real data. Unpaid orders are excluded server-side by 9.6. **Note:** the fix removing `shortId` and `line.grad` is in `components/shop/OrdersScreen.tsx` and was still uncommitted as of 2026-08-13. |
| 9.3 | "Saved addresses" screen | DONE | `AddressForm` and `AddressCard` extracted from `CartScreen` into `components/shop/address.tsx`; `CartScreen` and the new `AddressesScreen` both import them, and `CartScreen` lost ~138 lines rather than gaining a copy. **The mode split is a discriminated union, not a boolean:** `{ mode: 'cart'; selected; onSelect }` vs `{ mode: 'manage'; onSetDefault }`, so cart selection semantics cannot leak into the management screen or vice versa — it is a compile error, not a convention. Manage mode has no radio and no `delivery-address` name; "Set as default" renders only for non-default addresses, because the server ignores clearing `isDefault` on the only default (roadmap.md §PUT /me/addresses) and a control that silently does nothing is worse than no control. Checkout gate unchanged: Place order is still disabled on `!selectedAddressId`. Verified by the user in the browser; extraction, mode separation and the checkout gate verified statically here. TypeScript clean. |
| 9.4 | "Contact us" page + site footer | TODO | Details supplied 2026-08-12: email `support@enerzeiafuturefarm.com`, phone `8826345094`, WhatsApp `8826345094` (same number). Tap-to-act links (`mailto:`, `tel:+918826345094`, `wa.me/918826345094`). **Confirmed by the user 2026-08-13:** the domain is theirs with a monitored `support@` mailbox, and that number does receive WhatsApp — so the three spellings floating around the repo (`enerzia-be`, the `enerzeia` package name, the `enerzieafuturefarm` git remote) are not a typo in the support address. **Business hours: SUPERSEDED.** They were not supplied initially and the standing rule was to omit them rather than invent a window. The owner confirmed on 2026-08-13 that `Mon–Sat, 9 am – 6 pm IST` is their real staffed window, so it is now published — see `SUPPORT_HOURS` in `Enerzia/lib/content/social.ts`, the single source. The original reasoning still governs any future change: delete the constant rather than let it go stale, because an unstaffed published window is worse than none. A response-time promise ("we reply within X") was never supplied and is still not to be invented. **Reachability decision (user, 2026-08-13):** 9.7 left the signed-out account dropdown showing only "Sign in", and that dropdown was the only route to this screen, so a signed-out visitor could not reach contact details at all. Resolved by adding a **site footer** — the shop had none — carrying the three channels on every screen at every width; note `.topbar-links` is `display:none` at ≤640px, so the top-bar nav was not a viable home. Contact us is **removed from the account dropdown** so the footer is the single entry point. No contact form: there is no endpoint to receive one. |
| 9.6 | `ListForUser` returns only paid orders (`$in` over placed/packed/shipped/delivered/cancelled) | DONE | A pending_payment order was appearing in the shopper's list showing "Total paid ₹449" next to "Payment pending". Changed from `$ne StatusExpired` to `$in` over the five paid statuses — fails safe (new statuses excluded until explicitly added). `ByOrderID` unchanged (returns any status). Updated `TestListForUserExcludesExpiredAndSortsByCreatedAt` → `TestListForUserReturnsPaidOrdersOnlyAndSortsByCreatedAt`. Added `TestByOrderIDReturnsPendingPaymentOrders` to pin the asymmetry. `make check` green; `internal/order` 97.9%; total 97.0%. |

## Phase 10 — Admin catalogue
**Phase 10 gate (backend), 2026-08-13: lint 0 issues · total coverage 96.1% ·
all tests green (race) · `go build ./...` clean.** Five admin endpoints plus the
upload-signature endpoint live behind a token namespace separate from the
shopper's, pinned by tests in both directions.

**Image upload closed on 2026-08-13, against the real Cloudinary account.** The
credentials landed and the whole path was exercised end to end: the signature
endpoint returned 200 with the API secret absent from the body; **Cloudinary
accepted our signature**, which is the first real proof that the SHA-1
signed-upload format is correct — it is computed from their documented
algorithm and no test could have caught a wrong parameter string, only their
API can; the editor's own file control uploaded and rendered the CDN
thumbnail; Save persisted `{url, publicId}` into Mongo; and a `GET` on the
stored URL returned **200 image/png**. Nothing about this path is now
unverified.

Two orphaned assets exist in Cloudinary from that verification
(`enerzia/products/giuay6r2…` via curl and `…/ehdb3dgif…` via the UI, both 89-byte
probe PNGs). Delete them from the Cloudinary console — nothing deletes assets
automatically, by design (schema.md §products).

**Frontend verified in a real browser** against the live API on 2026-08-13, not
from a subagent report: list renders and converts paise correctly; edit
persists and reverts with `price`/`mrp` unchanged through the rupee round-trip;
create returns 201 and the product opens in the editor; duplicate id shows the
409 message on the id field; price-above-MRP shows the server's exact 422
string; retire greys the row and removes the product from `GET /products`;
restoring returns it. **Image upload is the one path never exercised** — no
Cloudinary credentials exist, so the signature endpoint correctly answers 500
and the client's error mapping is code-reviewed only. Close this when the
credentials land.

**Two defects were found by that browser pass and fixed, both invisible to the
test suite and to `curl`:**

1. `nutrition.rows` serialised as `null` for a product with no rows, and the
   editor's `p.nutrition?.rows.map(...)` guarded the wrong link in the chain —
   so **a product created in the console could never be reopened in it**, and
   the console is the only way to edit or retire one. It survived every test
   because all nine seeded products have nutrition rows; only a freshly created
   product reaches the branch. The same `null` also reached the storefront PDP.
   Fixed on both sides, and `roadmap.md` §Catalogue now states that every array
   in every response is `[]` and never `null`.
2. The create form had no Active control, so a new product was published the
   instant it was saved — half-written copy, no photographs, straight into the
   shop grid. Active is now on the create form, defaulting to **unchecked**:
   drafting is the default, publishing is deliberate.


**User decision, 2026-08-12.** The `products` collection has had exactly one
writer — the seeder — so changing a price, a blurb or a photograph has meant a
code change and a deploy. This phase gives it a console: a separate Next.js app
in `enerzia-admin/` that lists, creates and edits products, including up to five
photographs each.

Three decisions taken with the user before any code:

- **Admin auth is email + password from the environment**, not shopper OTP and
  not a collection. `ADMIN_EMAIL` + `ADMIN_PASSWORD_HASH` (bcrypt). Rationale
  and the trade-off in `schema.md` §The administrator.
- **Images live in Cloudinary**, not in Mongo and not on the container's disk.
  The browser uploads directly with a signature this server mints; only URLs
  and public ids are stored. `roadmap.md` §`POST /admin/uploads/signature`.
- **The console is its own app** (`enerzia-admin/`), so admin code can never
  ship inside the customer bundle.

Contracts were amended first, in order: `roadmap.md` (§Admin added, `images`
added to the product object, §Auth notes the second token), `schema.md`
(`products.images`, §The administrator).

Traps to carry into every task below:

- **`_id` is never rewritten.** It is the join key of every cart line and order
  line ever written. `PUT` with a different id is a 422, not a rename, and
  `DELETE` retires (`active: false`) rather than deleting.
- **Seeding must not wipe uploaded photographs.** `Seed` sets `images` nowhere —
  not in `$set`, and not in `$setOnInsert` except as `[]` on a genuine insert.
- **`discountPercent` and `soldOut` are outputs.** Accepting them on a write
  would create a second source of truth for a number the shop shows.

| id | task | status | notes |
|---|---|---|---|
| 10.1 | `products.images` on the model, validated (≤ 5), exposed on the public product | DONE | `Image` type + `MaxImages = 5` in `model.go`; image validation added to `validateDetail` in `content.go`; `productDTO.Images []Image` after `grad` in `handler.go` (serialises as `[]`, never `null`); `$setOnInsert` gains `images: []` in `Seed` — not in `$set` so re-seeding never wipes uploads. Tests: validate accepts nil/empty/5, rejects 6/blank/http; handler asserts `"images":[]` on list+detail for products with none, and round-trips url/publicId/alt for a product with images; repo test asserts `$set` ∌ images and `$setOnInsert` ∋ images. lint 0 · catalogue 99.0% · total 97.0% · all tests green (race). |
| 10.2 | `internal/admin` — bcrypt login, admin token, `Require` middleware, per-IP failure limiter, `cmd/adminpass` | DONE | `token.go` (issuer "enerzia-admin", 12h TTL, role claim "admin", cross-issuer blocked both ways); `limiter.go` (10 failures/15 min, injectable clock, prune on Allow, IP from `r.RemoteAddr` only — X-Forwarded-For NOT used); `service.go` (dummyHash generated at same cost as configured hash so timing is equal; limiter checked before unconfigured so both are indistinguishable; malformed hash → ErrNotConfigured, no panic); `middleware.go` (RFC-7235 case-insensitive bearer, `httpx.WriteError` envelope); `handler.go` (422 field order: email then password; messages verbatim from roadmap; canonical `h.service.Email()` in response; `net.SplitHostPort` for remoteIP; `/admin` subrouter — PathPrefix("/admin/mexyz") does not match /me); `cmd/adminpass` (`make admin-password`); config gains `ADMIN_EMAIL` (trimmed) + `ADMIN_PASSWORD_HASH` (NOT trimmed) both required in production. lint 0 · internal/admin 91.7% · total 96.6% · all tests green (race). |
| 10.3 | Admin product CRUD — repository, service, handlers | DONE | `ListAll`/`Create`/`Update`/`Retire` in repository; `ErrDuplicateProduct` via `mongotest.Reply`; `ValidateForWrite` (11-row table + publicId, id regex compiled at package init, separate from `Product.Validate`); `Writer` interface + `AdminService`; `AdminHandler` (5 routes behind `admin.Require`, admin DTO includes active/position/rating/badges/nutrition, discountPercent/soldOut rejected on write with 422, PUT id mismatch 422, Retire via `$set {active:false}` never delete); `AdminCatalogue` in `server.Deps`. lint 0 · internal/catalogue 95.4% · total 96.1% · all tests green (race). |
| 10.4 | `internal/cloudinary` + `POST /admin/uploads/signature` | DONE | `Signer` interface (`SignUpload(timestamp int64) (Signature, error)`), `Client` (SHA-1 of `folder=<f>&timestamp=<t><apiSecret>`), `Unconfigured` (`ErrNotConfigured`). Config gains `CLOUDINARY_CLOUD_NAME`/`CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET` (required in production, NOT trimmed for secret) + `CLOUDINARY_FOLDER` (optional, defaults to "enerzia/products"). Handler placed in `internal/admin` (owns the /admin subrouter — avoids a third package for one endpoint); `Handler.WithCloudinary(signer, now)` builder for backward-compat wiring; `ErrNotConfigured` → 500 logged at ERROR. Validate fix-up: URL check now returns `"Image links must start with https://."` not the count message. lint 0 · internal/cloudinary 100% · total 96.0% · all tests green (race). |
| 10.5 | Wire into `cmd/api/main.go`, `.env.example`, `make check` green | DONE | Cloudinary signer (real when `CLOUDINARY_CLOUD_NAME != ""`, else Unconfigured), admin token issuer over `cfg.JWTSecret`, admin service + handler + `WithCloudinary`, catalogue admin handler reusing existing `catalogueRepo`. Startup log: `admin configured signIn=<bool> cloudinary=<bool>` — booleans only, no email/hash/key. Router test `TestAdminRouteIsRegisteredAndProtected` asserts 401 not 404 for `GET /api/v1/admin/products`. Signer test fix-up: `independentSHA1` deleted; both expected digests replaced with external reference values from `shasum -a 1`, with comment forbidding regeneration from own output. `.env.example` already complete from 10.4. lint 0 · total 96.1% · all tests green (race). |
| 10.6 | `enerzia-admin/` scaffold + login screen | DONE | New app at `enerzia-admin/`. Next.js 16.3 + React 19.2 + TypeScript 5.9, matching Enerzia/ versions. Dev port pinned to 3001 (`next dev -p 3001`). `lib/api/`: `config.ts` (NEXT_PUBLIC_API_BASE_URL), `errors.ts` (ApiError/NetworkError/guards), `http.ts` (request<T>, envelope unwrap, global 401 hook header-gated), `token.ts` (**sessionStorage** not localStorage — admin token opens write access, must die with the tab; comment explains why), `types.ts` (LoginData, MeData, AdminProduct with all fields for 10.7), `auth.ts` (login, me). Login page: email + password form, server's error message verbatim (401/429/422 fields[0]), button disabled during flight, re-enables on both success and failure, type="password" + autoComplete="current-password", never logs password. Session restore: on load call /admin/me if token present; expired token → clear + back to /login. Products page: auth guard on mount (no token → /login; /admin/me fails → clear + /login), sign-out button, placeholder for 10.7. Root / redirects to /login (server-side). CORS trap noted: user must add http://localhost:3001 to ALLOWED_ORIGINS in enerzia-be/.env. `typecheck` exit 0, `build` succeeds. **Browser checks — verified:** (1) sign-in renders at http://localhost:3001 — HTTP 307→/login confirmed via curl, page serves HTML with form. **NOT VERIFIED in browser (no running binary with admin routes):** (2) wrong password shows server message + button re-enables; (3) correct sign-in lands on /products; (4) reload /products stays signed in; (5) close tab signs out (sessionStorage); (6) no console errors — structural hydration issues are unlikely given no SSR mismatch patterns, but not confirmed live. **Phase 10 backend gate: lint 0 · total 96.1% · all tests green (race).** |
| 10.7 | Product list, create, and the editor incl. 5-image upload | DONE | `lib/api/products.ts`: listProducts, getProduct, createProduct, updateProduct, retireProduct, getUploadSignature. `components/PageShell.tsx`: shared header + sign-out wrapper. `/products` list: table with id, name, form, price, mrp, stock, image count; retired rows greyed + "Retired" badge visually distinct; empty and error states distinct ("no products" vs "could not load"). `/products/new`: all fields — id (permanent, warned), name, family, form (select), stat/stat2/blurb/grad, mrp/price in rupees (Math.round×100 on submit), stock, position, rating, images (upload flow), badges (add/remove), nutrition rows (add/remove); 409 → error against id field; 422 → fields[0].message; 201 → navigate to editor. Active checkbox defaulting UNCHECKED (product stays hidden until deliberately published). `/products/[id]` editor: same fields, id read-only; GET to load, PUT to save (discountPercent+soldOut stripped; id sent unchanged not changed); stock field warns it overwrites reservations; active checkbox explains retirement and restoration; retire section behind in-page confirm ("stops appearing in shop, not deleted, past orders still resolve", "to restore: check Active and save"); upload failure distinguishes 500-from-our-server (Cloudinary not configured) vs Cloudinary-rejection vs network error; image "Make primary" moves to index 0. Money trap handled: paise÷100 to display, Math.round(rupees×100) to send, never float. `styles/admin.css` extended with table, form sections, image grid, dynamic rows, save bar, retire confirm, success/danger states. `typecheck` exit 0, `build` succeeds (6 routes). **Fix-up applied (pre-browser-verification):** (a) Backend: `nutrition.rows` serialised as `null` when no rows present — added `normaliseNutrition` helper to `handler.go`; both shopper `getProduct` and admin `toAdminProductDTO` now call it; `getTrust` wraps tiles in `orEmpty`; two new tests assert `"rows":[]` on raw JSON (struct comparison would miss this since Go decoder maps both `[]` and `null` to nil slice). lint 0 · catalogue 95.4% · total 96.1% · all tests green (race). (b) Frontend editor: `p.nutrition?.rows.map(...)` → `p.nutrition?.rows?.map(...)` (double-guard); save error div gains `role="alert"`. (c) Create form: `active` added to Draft (default `false`), `buildBody` uses `d.active` not hardcoded `true`; Active checkbox added defaulting UNCHECKED with hint "stays hidden until ticked"; error div gains `role="alert"`. **Awaiting browser re-verification (7 checks with live API).** **Test data in live Atlas:** `zz-test-product` and `zz-test-two` were created during browser verification and left RETIRED — see task 7.11 for pre-production cleanup. |
| 10.7-cleanup | Remove `zz-test-product` and `zz-test-two` from live Atlas before launch | TODO | Both products were created via the admin UI during task 10.7 browser verification and retired afterwards. They are inert (not shown in the shop) but are real documents in the `enerzia` Atlas cluster. Delete them manually (Atlas UI or mongosh) as part of the pre-production data purge described in task 7.11. |

## Phase 8 — Sign-in via the MSG91 OTP widget

**User decision, 2026-08-09.** DLT registration was blocking launch, so sign-in
moves to MSG91's OTP widget, which sends on MSG91's own default template until
your DLT template is approved. Contracts amended first: `roadmap.md` §Auth
rewritten, `otp_codes` marked removed in `schema.md`.

Trap to carry into every task: MSG91 signals success with `type` in the body,
**not** the HTTP status, and returns the phone **with its `91` country code**
while `users.phone` is 10 digits.

| id | task | status | notes |
|---|---|---|---|
| 8.1 | `POST /auth/session` — verify the widget access token, upsert, issue our JWT | DONE | New package `internal/msg91` (`Verifier` interface, `*Client`, `Unconfigured`); `ErrNotConfigured`/`ErrVerificationFailed` sentinels. `CreateSession` on auth service; `normalizePhone` strips leading `91` from 12-digit strings then validates 10 digits with `ValidPhone`. TRAP covered: `TestVerifyAccessToken/type_error_with_HTTP_200_is_a_rejection` — MSG91 returns HTTP 200 with `type:"error"`; we check `result.Type != "success"`, not status. 422 for empty/whitespace accessToken; 401 for every rejection (one message); 502 for unreachable MSG91. `MSG91_AUTH_KEY` required in production, optional elsewhere (falls back to `Unconfigured`). `internal/auth` 94.5%, `internal/msg91` 95.5%, total 97.0%. `make lint` clean (one `//nolint:gosec` for the outbound access-token marshal). |
| 8.2 | Delete the in-house OTP machinery | TODO | `otp_codes` model/repository, HMAC pepper, attempt counter, single-use guard, `auth.Sender` + `LogSender` + `UnconfiguredSender`, the resend rate limit (6.1), and `devCode`. Config loses the OTP pepper and gains `MSG91_AUTH_KEY` |
| 8.3 | Frontend: `LoginScreen` onto the MSG91 widget | DONE | New `types/msg91.d.ts` (optional `window.initSendOTP`). `lib/api/auth.ts` adds `exchangeSession`. `lib/api/types.ts` adds `SessionData`. `lib/shop/types.ts` adds `'widgetSession'` to `RequestKey`. `lib/shop/pricing.ts` adds `widgetSession: false` to `allPendingFalse`. `useShop.ts` adds `signInWithWidget` command (same post-verify sequence as `verifyOtp`). `ShopClient.tsx` loads the MSG91 script on login screen with ready flag and error banner. `LoginScreen.tsx` replaces phone/OTP two-stage form with a single button that calls `initSendOTP`; re-checks global at click time for ad-blocker guard; banner used for all errors. TypeScript clean (exit 0). Browser checks 2/4/5/6 need a real phone to verify — see task notes below. |
| 8.4 | Drop `otp_codes` from live Atlas incl. its two indexes | TODO | `EnsureIndexes` creates but never removes — manual drop, same as M.2 and M.5 |
| 8.5 | Live smoke test of the new sign-in | TODO | Needs a real Indian number; MSG91's default template sends without DLT. **Do 8.6 first.** |
| 8.7 | **BLOCKED: MSG91 rejects a valid access token with `418`** | BLOCKED | Sign-in fails after a *successful* OTP. `verifyOtp` returns `{"type":"success","message":"<JWT>"}`, we post that 160-char JWT to `verifyAccessToken` with the account authkey, and MSG91 answers `{"message":"AuthenticationFailure","type":"error","code":"418"}`. **Ruled out by direct testing:** the authkey is valid (a wrong key gives `201`, ours gives `418`); the request body, URL and header match their docs; the token is complete, not truncated; both `api.` and `control.msg91.com` behave identically; and the IP allowlist is NOT the cause — the call fails identically from an IPv4 address MSG91's dashboard shows as **Whitelisted**. (An MSG91 KB article attributes `418` to IP allowlisting, but that is documented for the SMS-sending APIs and demonstrably does not transfer to this endpoint.) Leading remaining hypothesis: server-side `verifyAccessToken` requires an active **OTP Widget subscription** and the account is on the free tier. Needs MSG91 support to confirm — this is not resolvable from the code. **Confirmed 2026-08-17 by a controlled probe, and the token is now ruled out entirely.** Posting a *deliberately invalid* access token to `/api/v5/widget/verifyAccessToken` returns **418 with our real authkey** but **201 with a made-up authkey**. The token was equally garbage in both calls, so the differing response cannot be a judgement about the token — MSG91 recognises our key (an unknown key answers 201) and then refuses it for this endpoint specifically. That eliminates token truncation, the widget flow, the JWT contents and all of our code, and leaves an **account-entitlement problem on the key itself**. Reproduced identically on `control.` and `api.msg91.com`. Live sign-in attempts on 2026-08-17 14:41 and 14:42 IST logged `msg91_code:"418"`, `token_length:160` → our API correctly answered 401. **This blocks all revenue** — the cart is auth-gated, so nobody can check out while it stands. |
| 8.6 | **MSG91 panel configuration (user-side, no code)** | TODO | (a) Set the widget to **mobile only** — email verification returns an email as the identifier, and `users.phone` is the identity, so there is nothing to key the account on. The server rejects it correctly, but the shopper sees a baffling failure *after* MSG91 told them they succeeded. (b) The widget's hCaptcha warns **"localhost detected, please use a valid host"**, so a live send likely needs a tunnel or a deployed host. Both are panel/hosting settings — invisible from the codebase, and nothing in it will explain the symptom. |

**Correction to the provider comparison (2026-08-09):** MSG91 was recommended
partly on "no captcha" and "works on localhost". Both were wrong — the widget
uses hCaptcha and warns on localhost, exactly like Firebase. The recommendation
still holds on the reason that actually mattered (India-native, and their
default template bridges DLT while registration is pending), but those two rows
were presented as differentiators without being verified.

| 7.13 | **A failed boot must not be a dead end** | DONE | Boot body extracted to `runBoot`; the `booted` StrictMode ref is left intact and retry calls `runBoot()` directly rather than resetting it. New `bootStart` action re-arms `booting` and clears the stale banner. `Banner` gained an optional `onRetry`, passed only when `products.length === 0 && !booting`, so a cart or PDP error never offers a button that re-runs the whole boot. **Fix-up during verification:** `trustLoaded` was clearing `banner` unconditionally, so whenever the trust request won the race against a failed `/products` the message — and with it the Retry button — was wiped, leaving an empty grid with no explanation at all. That one line is gone; `bootStart` is now the only boot-time clear. Audited the rest of the reducer: `reqFail` is the sole setter, `bootStart` and `clearBanner` the only clears, and `signOut` correctly preserves the message. Files: `lib/shop/reducer.ts`, `app/shop/useShop.ts`, `app/shop/ShopClient.tsx`, `components/shop/controls.tsx`, `styles/shop.css`. TypeScript clean. Verified in a real browser against the live backend — full outage, partial outage (`/products` 500 with trust 200), recovery on Retry in both, and Retry correctly absent on a non-boot PDP failure. |
| 7.12 | **Browse-and-add without a sign-in interruption** | TODO | **Reverses the 7.5 auth-gate decision after user testing.** Gating every Add behind sign-in was my recommendation (it avoided cart-merge rules) and it reads as hostile in practice — a shopper picking three things is bounced to an OTP screen on the first one. Replaced by: signed-out adds accumulate in a local intent buffer, a prompt offers "Go to cart", and sign-in replays the intents as `POST /cart/items`. No merge semantics, because there is never a second cart — replaying reproduces exactly what signing in first would have done. |
| 7.11 | **Pre-production data cleanup of live Atlas** | TODO | Development has been running against the live `enerzia` database throughout, so it now holds test users (throwaway phone numbers), test addresses, smoke-test orders (`EFF-958234` and any from 7.7/7.8 testing), and stock decremented by test purchases. Purge users/carts/addresses/orders and re-seed product stock before real customers arrive. **Do not skip** — a test order in a real order history is indistinguishable from a real one to whoever reads it later. |
| 7.10 | Reconcile `roadmap.md` with the code + QA script | TODO | The 12 discrepancies found during exploration (user's decision: doc follows code). Note `?form=` and `detail.siblings` go unused by this client. |
| 7.14 | Mobile touch-target audit and fix | DONE | Audited at 375px with touch emulation across `/`, `/shop`, a PDP, the login screen, `/contact`, `/terms` and `/faq`. **No horizontal overflow anywhere and no element past the right edge** — the layout was sound. Fourteen interactive controls were under 44px: the announcement dismiss (27×26), the shop's category filters (29px tall — the most-tapped control on the page), the PDP quantity stepper (34×34), the account button, the cart button, the burger, the hero trust chips, and every "Back to shop" link (17–36px). Fixed with one `@media (pointer: coarse)` block keyed off the POINTER, not viewport width, so a phone gets full targets, a desktop keeps its density, and a large touch tablet is covered too — a `max-width` query would have missed the last case. Verified after: desktop `.pill` still 43×29 and `.btn-account` still 36×36, so nothing leaked. The announcement dismiss was raised to 44×44 unconditionally, being tight even for a mouse. Two selectors initially missed their target because `.legal-back-bottom` and `.contact-back` are wrapping `<p>`s, not the links — caught by re-measuring rather than assuming the rule applied. Typography checked and left alone: legal body text is 14.5px/24.65px, which reads fine. |
| 7.15 | Header simplified, footer gap on mobile, card hover | DONE | **Footer gap:** the mobile block set `.footer-outer { padding: 0 14px 32px }`, zeroing the top padding, so the panel butted straight into whatever section preceded it. Restored to 32px. **Header:** hamburger, mobile menu, and the Benefits / Our Farm entries removed. The nav is now one link — Home offers "Shop", the shop offers "Home" — so it fits at every width and needs no burger; hiding a single link behind one costs a tap and buys nothing. `.nav-pending`, `.site-burger` and all `.site-menu*` CSS deleted along with the `NavItem` union's `pending`/`action` kinds, rather than left as dead abstraction. The nav link needed adding to the touch-target block: it had been hidden behind the burger on mobile, so 7.14's audit never saw it. **Card hover:** product cards, the homepage "Living with it" cards and the review cards scale to 1.02 with `--shadow-md`; the inner art scales 1.06 inside its clipped frame so the photo moves relative to the card. Gated on `@media (hover: hover)` — an unscoped `:hover` sticks after a tap on touch — and `z-index: 1` keeps the scaled card above its neighbours. Reduced-motion drops the movement but keeps the shadow, which is the actual affordance. **Measurement note:** hover verification first appeared to fail — computed transform stayed identity and the shadow unchanged while `z-index` applied. The cause was the browser pane being throttled, so transitioned properties never advanced from their start values while untransitioned ones applied instantly. Disabling the transition and re-measuring showed the correct `scale(1.02)` / `scale(1.06)`. The CSS was right the whole time. |
| 7.17 | **Protein figure 60% → 62%, heavy-metals badge, real homepage hero** | DONE | (a) **62% everywhere**, matching the printed jar label: frontend `home.ts` (hero badge + chip), `ShopScreen.tsx` trust tag; backend `catalogue/seed.go` (two product `Stat`s + the trust tile) with the pins in `seed_test.go` and `handler_test.go` updated; docs `product.md`/`schema.md`/`roadmap.md`. **`repository_test.go`'s "60%+" was deliberately left** — it is a local fixture asserting the repository round-trips whatever is stored, not the real seed value, and that file is the catalogue agent's. The trust-tile body's "dried below 60 °C" is a temperature and was correctly untouched. **`make seed` was run against live Atlas** so the change actually reaches the site — the code change alone does nothing, since both values are served from Mongo. Verified live: `/content/trust` → `"62%+"`, `/products` → `"62% plant protein"`. (b) **Heavy-metals badge** added as a hero chip, worded **"Heavy metals tested", not "no heavy metals"** — a lab report shows quantities below the permitted limit and cannot show zero, so the absolute form is a claim no report on file supports, which on an FSSAI-licensed food is the difference between substantiated and false. A matching `Heavy metals` seal was added to §proof because the chip links to `#proof` and a badge pointing at a section that never mentions it is worse than no badge; `.home-seals` moved to `auto-fit` since a hardcoded `repeat(4,1fr)` stranded the fifth, and the track floor is 125px so two still fit at 375px (140px collapsed it to one very tall column). (c) **Homepage imagery: hero + the three "Living with it" cards are now photographs** rather than gradients. All four are **AI-generated still-lifes** supplied by the owner, and the boundary is written into `home.ts`: images that only illustrate *use* (a smoothie, a bottle in a bag, a shaker, powder in a bowl) may be generated; anything depicting the farm, ponds, process, people or the pack **must be real**, because that is what the brand's traceability claim rests on. No generated image shows a branded pack — a generator invents a label, and an invented Enerzeia jar is worse than none. Files live in `/assets/home/` (generated) kept separate from `/assets/farm/` (documentary), converted to JPEG q68 — 9.5 MB of PNG down to **1.0 MB**. The real `pond-raceway-wide.jpg` remains the one-line alternative for the hero. `.home-moment-art` moved from 4:3 to **1:1** because all three sources are portrait and a landscape frame cropped away the bottom of each, which is where the subject sits. (Earlier in this task the hero was briefly the real pond photo.) `FarmPhoto` was promoted to a shared `components/Photo.tsx` with a `frameClassName` API, and the `Photo` type moved to `lib/content/types.ts`, so the homepage inherits the pre-hydration fallback rather than duplicating it. `ProcessStep.photo` is now **required** — the design rule turned into a compile error. |
| 7.16 | **"Our Farm" page (`/farm`) + nav entry** | DONE | New route `app/farm/page.tsx` (server component), copy in `lib/content/farm.ts`, photo component `components/farm/FarmPhoto.tsx`, styles appended to `styles/shop.css`. Sections: hero, three ownership points, a 7-step process walk, "why the grade holds" (4 cards + wide establishing shot), the byte, farm visits → `/contact`, closing CTA → `/shop`. Nav entry added to the homepage and the shop (`TopBar`); `/farm` itself offers Home + Shop. **Photographs: 7 of 9 slots filled.** The owner supplied them as phone PNGs (~20 MB total, far too heavy for one page); converted with `sips` to JPEG q68 / 1400px long edge → **2.7 MB**, no visible loss, and the untouched originals moved to `Enerzia/assets/farm-originals/` (outside `public/`, so not served). Crops verified against the real frame CSS. **`harvest-hall.jpg` and `farm-visit.jpg` were never supplied**, so the page was **restructured (user request) to use exactly the seven photographs that exist** — no gradient slots, no photo-less steps. Four changes: (a) "checked by hand daily" merged into the paddlewheel step, since both describe the same stage, leaving all four process steps with a photograph; (b) the two post-harvest stages (drying/milling, lab testing) moved out of the alternating rhythm into a compact 2-up card pair — an image/text alternation with no image in it reads as a missing photo, not a design; (c) the grade section lost its wide establishing shot and stands on its four cards; (d) the visits section was deleted and its invitation folded into the closing block as a note plus link. Net: 9 photo slots → 8, **all filled, no gradients anywhere**; page height 6420px → 5772px. Two more photographs arrived afterwards and were folded in: `product-jars.jpg` (the two tablet jars) became the closing image — the only studio shot on the page, placed last because that is where the farm story hands over to the shop — which freed the biomass bowl to fill the grade section beside its four claims; and `drying-strands.jpg` (dried strands on cloth) let the drying stage move **out** of the text-only cards and back into the photo rhythm as step 05, leaving lab-testing as the single remaining card. `.farm-finish` uses `repeat(auto-fit, minmax(300px,1fr))` rather than a fixed two-column track precisely so one card spans instead of being stranded in the left half. The remaining photo-less step ("Lab tested, then sealed") was then **dropped in favour of a "Book a visit" panel** (user, 2026-08-16), which also became the single home of the visit ask — the duplicate note in the closing block was removed, so the page asks once. **The testing claim was not lost**: it still runs verbatim in `OWN_FARM_POINTS` ("an independent NABL-accredited laboratory … not a sample, every batch") and again in `GRADE_POINTS`, so this cost a repetition rather than a claim. The sealing half moved into step 05's copy, which keeps `PROCESS_INTRO.heading` ("…to a sealed pack") honest. `FINISHING`, `VISIT_NOTE`, `.farm-finish*`, `.farm-visit-note` and `.farm-visit-link` all deleted. **"Book a visit" is a button label, not a booking system** — the body says plainly that you get in touch and a time is arranged; do not add hours or a slot picker unless they exist, the failure mode is people arriving at a gate. **Final state: 9 photographs, 9 slots, none empty; 5 photo steps, every one with a picture; 3.0 MB of imagery; zero undersized touch targets at 375px.** `.farm-visit*`, `.farm-step--textonly`, `.farm-photo--wide` and the `'wide'` shape were deleted rather than left as dead abstraction. **Do not substitute another photograph** if those two ever matter again: each `alt` describes its specific shot, and the visit copy claims people stand at the pond edge. Manifest and the conversion command are in `public/assets/farm/README.md`. One touch-target fix during the restructure: the visit link was 141×17 inline in a sentence, so it moved onto its own line (an inline link cannot take a 44px box without stretching the line box — the same trap 7.14 hit with the wrapping "Back to shop" paragraphs); now 162×44, and the page has zero undersized targets at 375px. **Three defects found in verification, none visible to a typecheck:** (1) `onError` alone never fired on the SSR'd hero image — the 404 lands before hydration attaches the handler, so the frame showed sprawled alt text over the gradient; fixed with a mount-time `complete && naturalWidth === 0` check. (2) The second nav link pushed the shop's mobile header past its width — measured by removing the link in the DOM, which moved the cart's right edge from 391px back to 355px on a 390px screen. (3) The first fix for that put the media query ABOVE `.site-nav`, so the later `gap: 26px` won — a media query adds no specificity. **Superseded during the task (user):** tightened gaps still left both labels wrapping to two lines at 375px, so the **burger is back below 560px** — reversing 7.15, which removed it when the nav was one link. Two links + account + cart need 417px against 375px of screen, so the arithmetic changed, not the principle. Menu is accessible (`aria-expanded`/`aria-controls`, Escape closes and restores focus, click-outside closes, 44px trigger, 48px rows) and all four behaviours were verified live. **Byte attribution settled (user, 2026-08-16): "Founder" alone** — no personal name, no company line beneath it. `BYTE.role` and `.farm-byte-role` deleted rather than left unused; do not "complete" the attribution later by adding a name, it is deliberately anonymous. The **quotation itself is still copy drafted here, not a transcript** of anything said aloud; the owner has reviewed the page with it in place and it stands until they supply their own words. The standing rule from `Testimonial` in `lib/content/types.ts` applies: nobody else puts new words into that person's mouth. |
| 7.12 | Offline cart buffer: let a signed-out shopper add several products without a login interruption | DONE | `cartBuffer: BufferItem[]` replaces the single-intent `pendingCartIntent`. localStorage key `enerzia:cart-buffer` persists the buffer across reloads. `addItem`/`addFromPdp` append/increment the buffer when signed out (no `requireAuth`). Grid stepper shows `"n selected"` for buffered items. Green `.selection-prompt` strip below the top bar shows total count + "Go to cart →". Cart badge counts the buffer when signed out, server cart when signed in. `verifyOtp` replays the buffer in order after sign-in and reports any sold-out failures by name. Buffer cleared on sign-in replay and on sign-out. Files: `lib/shop/types.ts`, `lib/shop/reducer.ts`, `lib/shop/pricing.ts`, `app/shop/useShop.ts`, `app/shop/ShopClient.tsx`, `components/shop/ShopScreen.tsx`, `components/shop/PdpScreen.tsx`, `components/shop/controls.tsx`, `styles/shop.css`. TypeScript clean. All 7 browser checks passed (Playwright/Chrome). |

---

## Phase 13 — Deployment

Owner request (2026-08-17): deploy to a VM, `enerzeiafuturefarm.com` →
storefront, `admin.enerzeiafuturefarm.com` → console. Backend containerised or
not was left to me.

| id | task | status | notes |
|---|---|---|---|
| 13.1 | Deployment artifacts + runbook | DONE | `deploy/` at the repo root: `Caddyfile`, three systemd units, `api.env.example`, `deploy.sh`, `RUNBOOK.md`. **Chose systemd over containers**: three processes, one box, one operator — Docker would add a daemon, image builds and a registry, and put a layer between the operator and `journalctl` at 11pm. The Go binary is static and Next needs Node either way, so containers buy little here; that calculus flips at more than one box. **Caddy over nginx+certbot** for automatic issuance and renewal in six lines. Atlas stays external, so the VM is stateless and rebuildable. `deploy.sh` builds all three **before** restarting any, so a compile error leaves the running site untouched, and health-checks all three ports afterwards. Units bind to localhost only; Caddy is the sole public listener; `ProtectSystem=strict`, `NoNewPrivileges`, a dedicated unprivileged user, and a sudoers rule scoped to exactly three `systemctl restart` commands. `StartLimitBurst` set because the API deliberately refuses to boot on bad production config and an infinite restart loop would bury the one line saying why. |
| 13.2 | Run the deployment | TODO | **Owner.** Needs a VM, DNS, and production credentials. See `deploy/RUNBOOK.md`. |
| 13.3 | Verify the Razorpay webhook post-deploy | TODO | The whole reason 11.11 is unverifiable today: Razorpay rejects tunnel hostnames and has never reached this machine. First deployment is the first chance. Configure the webhook, place one ₹1 live order, confirm `payment.method` is stamped, refund by hand. |

---

## Phase 12 — Order confirmation email

Owner request (2026-08-17): email the customer on order confirmation, with an
invoice attached. Provider **plain SMTP**, document **a real GST tax invoice**
(both owner decisions).

**Three units, not one**: the SMTP sender, the confirmation email, the GST
invoice. Only the first is unblocked.

| id | task | status | notes |
|---|---|---|---|
| 12.1 | `internal/email` — SMTP sender | DONE | `net/smtp` + `mime/multipart`, both stdlib, so **`go.mod` stays at four direct dependencies**. `Message` + `Sender` + `Unconfigured`, matching the shape Razorpay/Cloudinary/MSG91 already use. Built by hand rather than via `smtp.SendMail`, which cannot do implicit TLS on 465 and never lets the caller *require* an upgrade. **STARTTLS is required, not attempted**: an order confirmation carries a name, address and phone, and silently falling back to plaintext because a server did not advertise the extension is exactly how that leaks — a test asserts the client sends no envelope data at all in that case. No `InsecureSkipVerify` anywhere; the test trusts a self-signed CA explicitly instead. `Validate` rejects newlines in the subject and recipient — **header injection**, and the subject is built from partly shopper-supplied order data. MIME layout collapses to the simplest form that fits (mixed → alternative → single part), because a single part wrapped in pointless layers renders as an empty mail in some clients; plain part is written **before** HTML, since a client picks the *last* part it can render. Base64 wrapped at 76 columns, with a test that no line exceeds the 998-octet limit. `Send` takes a `context.Context` and dials with `DialContext` so a shutdown cancels an in-flight connect — the linter caught `net.DialTimeout` and it was right. Tests run against an **in-process fake SMTP server over `net.Pipe`**, including a real TLS handshake on both 465 and 587, so the suite stays offline — same posture as `internal/mongotest`. Config: six `SMTP_*` vars, all-or-nothing (a from-address with no host sends nothing at all, silently), port defaults to 587. lint 0 · internal/email 86.6% · config 100% · total 95.5% · `make test` green (race). **Verified against the real Gmail host, not a fake**: connect → STARTTLS → AUTH succeeded as `orders@enerzeiafuturefarm.com` (checked first with a connect-and-quit that sent nothing), then one real message to an owner-nominated address — subject with a rupee sign and en dash decoded correctly, the HTML alternative was chosen over the plain part, and the attachment opened. Owner confirmed all checks. **Google note:** app passwords survived the May 2025 retirement of less-secure-app sign-in but only exist when 2SV is on, and are blocked outright when the Workspace enforces security keys — if this path ever disappears, `smtp-relay.gmail.com` or a transactional provider is the same six env vars and no code change. |
| 12.1-cov | `internal/email` sits at 86.6%, below the 90% floor | NOTE | The uncovered statements are `if err != nil` returns on `multipart` writes against a `bytes.Buffer`, which cannot fail. The two reachable ones (an over-length MIME boundary; a server rejecting MAIL/RCPT/DATA) **are** covered. Raising this further would mean injecting a failing `io.Writer` purely to execute unreachable error paths, which pads the number without earning it. |
| 12.2 | Order confirmation email on payment capture | DONE | `BuildConfirmation` in `internal/order/confirmation.go` + a `Notifier` hook on the service, wired in both the callback and webhook paths. **Exactly-once came free**: both go through the same guarded `MarkPlaced`, so the send is gated on `modified` — the webhook can be redelivered any number of times and the customer gets one email. A dedicated test pins the losing path sending **zero**. **Built from the post-transition view, not the pre-read `o`** — emailing `o` would tell a paying customer their order is still awaiting payment. **Sent on a detached goroutine** with `context.WithoutCancel`: the shopper is waiting on the callback and Razorpay needs a fast webhook answer, neither should hold open for an SMTP round-trip, and a returning response must not abort a half-sent email. `WithoutCancel` rather than `Background` keeps the request id in the log line — the linter's `contextcheck` caught the original and was right. **Mail can never fail a payment**: every error is logged and swallowed, with a test that a refused SMTP connection still returns a placed order. `ErrNotConfigured` logs at Debug, not Error — mail being switched off is not a fault worth an ERROR line per order. Both bodies rendered; `html/template` escaping tested against a `<script>` in a shopper-supplied name; a test asserts no Razorpay id reaches a customer inbox. **Rupee grouping written by hand** because Indian grouping is last-three-then-twos — a naive formatter renders ₹12,34,567 as ₹1,234,567, a different number to the person reading it; six values pinned. Zero savings omits the row and free delivery says "Free", not "₹0". `main.go` selects `email.Unconfigured` when SMTP is unset and logs `mail=false` at boot. lint 0 · internal/order 96.5% · total 95.4% · `make test` green (race — which matters, the send is on its own goroutine). |
| 12.5 | A failed confirmation email is lost | TODO | Discovered while building 12.2, and a deliberate limitation rather than an oversight. The send is best-effort with no retry: if SMTP is down for the thirty seconds after a capture, that customer has paid and will never hear from us, and only an ERROR log records it. Fixing it properly needs somewhere to record intent — a `confirmationSentAt` on the order, or a small outbox collection — plus a worker, which is the same shape as the existing reservation sweeper. **That is a `schema.md` change, so it needs the owner's decision before I build it.** | **Exactly-once is already solved**: both the callback (`service.go` `MarkPlaced`) and the webhook (`webhook.go`) go through the same guarded update returning `modified`, and the cart clear already hangs off it. Send beside it, gated on the same flag — one email per order however many times Razorpay retries. Must be **best-effort**: a mail failure is logged and never rolls back or fails a payment confirmation. Recipient is `order.shippingAddress.email`, **not** the account — two orders from one shopper can legitimately go to different addresses. Needs a record of what was sent so a resend is deliberate. |
| 12.3 | GST tax invoice attachment | PENDING (owner, 2026-08-17) | **Deferred, not cancelled.** The confirmation email keeps carrying the order summary in its body and nothing is attached. When this resumes it becomes an attachment on the same email — 12.2 needs no change, since `email.Message` already supports attachments and that path is tested. Still needed before it can be built, and none of it is derivable: **first**, whether the business is GST-registered at all — if not this is a *Bill of Supply* with no tax lines, not a tax invoice, and most of the config below is irrelevant; then GSTIN, registered legal name and address, GST state code, HSN (a CA's call — 2102 vs 2106 for spirulina, and it may differ per product, in which case it belongs in the catalogue rather than env), the rate, and **whether stored prices are tax-inclusive** — at 18% inclusive ₹200 is ₹169.49 + ₹30.51, exclusive it is ₹200 + ₹36; the same displayed price, a different liability, and every invoice wrong if it is set backwards. **Also needs a schema change**: invoice numbers must be sequential and gapless, so the counter has to live in MongoDB with an atomic increment — env alone cannot do it, two concurrent orders would take the same number. That needs the owner's approval. **Compliance note, raised once and left with the owner**: a registered seller issuing no tax invoice is a live exposure, so this should close before launch rather than drift. | **Cannot be built from current data** — this was said before the choice was made and remains true. Needs from the owner: GSTIN; the HSN code for spirulina supplements (near 2102 vs 2106, and the classification drives the rate — a CA's call, not mine); the GST rate; whether stored prices are tax-inclusive (₹200 either contains the tax or adds it, and reversing that misstates every invoice and the liability); a **gapless sequential invoice-number series per financial year**, legally distinct from `EFF-######`; and the registered legal entity name and address. Derivable without asking: **CGST+SGST vs IGST** from `SHIP_FROM_STATE` against the delivery state, and place of supply = delivery state. Also needs a PDF library — the first new dependency — or the attachment ships as HTML. |
| 12.4 | SPF/DKIM on the sending domain | PART DONE — deliberately left (owner, 2026-08-17) | **DKIM is set up and verified**: `google._domainkey.enerzeiafuturefarm.com` returns a valid `v=DKIM1; k=rsa; p=…`, and MX confirms Workspace. **SPF is absent** — no `v=spf1` record at the root at all — and DMARC is absent. Owner decided to leave both. **That is defensible**: DMARC passes on either SPF *or* DKIM, Google/Yahoo require only one of the two below bulk-sender volume, and both test emails arrived. The cost is redundancy — DKIM is currently the *only* thing authenticating this mail, so if it ever breaks (key rotation, a DNS edit, Workspace reconfigured) authentication drops to zero in one step and the symptom is customers not receiving order confirmations. Also noted: an SPF record for Workspace is Gmail-specific and would be replaced wholesale by a provider migration, so adding it now is partly throwaway. **If deliverability is ever suspected, check this first.** |
| 12.6 | Move to a transactional email provider | TODO | Owner intent (2026-08-17): Workspace SMTP is a stopgap and a provider comes when volume justifies it. **Trigger**: Google caps Workspace at roughly 2,000 messages/day and throttles bursts; app passwords are also a mechanism Google keeps narrowing, and they vanish entirely if the account ever enforces security keys for 2SV. **Cost when it happens: six env vars and no code change** — `internal/email` speaks plain SMTP and every provider worth using offers it, so `SMTP_HOST`/`PORT`/`USERNAME`/`PASSWORD`/`FROM_*` is the whole migration. New SPF `include:` and a new DKIM key are the real work, both DNS. |

---

## Phase 11 — Order book & shipping labels

Contract: `roadmap.md` §Admin orders. Schema: `schema.md` §orders.

**Manual fulfilment, no courier integration.** Shiprocket was scoped and
rejected (owner, 2026-08-17): for a shop at this volume the value was in not
retyping addresses, and printing our own label gets that without a client, a
token refresh, an outbox worker or a third-party webhook to dedupe.
`product.md` §5 keeps listing logistics integration as out of scope.

**No cancellation and no refund** (owner, 2026-08-17). `cancelled` and
`delivered` stay valid stored values with no transition reaching either.

| id | task | status | notes |
|---|---|---|---|
| 11.1 | `Fulfilment` as a **separate field** from `status`, + the transition table | DONE | **Reworked mid-task on owner instruction (2026-08-17): fulfilment must not touch the shopper's `status`.** The first cut added `in_transit` to `Status`; that was wrong, and overloading one field would have meant an operator marking a box packed had rewritten what the order says about payment. Reverted in full — `Status` is back to its original eight values, `Status.Label()` back to plain names, `ListForUser`'s `$in` back to five. New `Fulfilment` type in `model.go`: `FulfilmentNone`/`Packed`/`InTransit`/`Shipped`, with `Valid()`, `Label()` (Not started/Processed/Transit/Shipped) and `CanFulfil(from, to)`. `Order.Fulfilment` carries `bson:"fulfilment,omitempty"` — **omitempty is load-bearing**: `FulfilmentNone` is the absence of the key, which is exactly why the split needed no migration and why every pre-existing order is already correct. `Order.Validate` gains two rules: the value must be known, and a non-none fulfilment is rejected on any status but `placed` (fulfilment progress on an unpaid order means a parcel went out for free). `CanFulfil` is **derived from a `fulfilmentOrder` slice**, not a hand-written pair list, so a future stage is inserted in one place and cannot exist in the sequence without also being permitted by the guard. **Tested by exhaustive sweep, not a picked table**: all 16 ordered pairs asserted against an allow-list of three — a picked table proves the three legal moves work, only the sweep proves nothing else does, which is the half that matters in a guard. Separate pins for self-moves (refused: a PATCH that silently no-ops tells an operator a parcel moved when nothing did), unknown values, and a backwards-travel check written independently of the allow-list so a reordering of `fulfilmentOrder` could not quietly permit un-shipping a parcel. `ListForUser` untouched and explicitly commented as such. lint 0 · internal/order 98.0% · total 96.2% · `make test` green (race). |
| 11.2 | **Per-address delivery phone** — backend + storefront | DONE | Owner decision (2026-08-17), replacing the original plan of freezing the account's number onto the order: the delivery contact belongs to the address, because a shopper sending a gift needs the *recipient* reachable at the door, not themselves. **Backend:** `auth.Address.Phone` (`bson:"phone,omitempty" json:"phone,omitempty"`), validated in `ValidateAddress` **immediately after email** — both are how this delivery gets contacted — reusing `ValidPhone` (10 digits, no +91) with its own message so a 422 names the right field. Added to `normaliseAddress`, which runs *before* validation, so a padded `" 9876543210 "` trims rather than 422s. **Required on every write**, so editing an older address is when the shopper supplies one; addresses saved before the field stay readable with `phone` absent, and checkout falls back to the account number rather than blocking a shopper on a field they never saw. Docs: `roadmap.md` §Addresses, `schema.md` §users, `product.md` §3.4 (validation table renumbered to six rows). Tests: four new rows in the message table (missing, too short, `+91`-prefixed, separators), the field-order walk extended to six steps, and the trim test now asserts phone. Eight JSON fixtures updated. **Storefront:** `AddressDTO.phone?` (optional — old addresses), `Address.phone` (required), `emptyAddr`, the edit-mapping (`existing.phone ?? ''`), both request bodies in `useShop.ts`, `validateAddress` in `pricing.ts` **in the same order and wording as the server** — a client validating in a different order shows a different message than the server would for the same input — and the field itself between email and street. Digit-stripping went in the reducer's `setAddr` beside the existing `pin` rule rather than in the component, matching how the codebase already does this; phone uses exactly the sign-in field's rule (`digits(v, 10)`). lint 0 · internal/auth 94.6% · total 96.2% · `make test` green (race) · `npm run typecheck` clean. **Not verified in a browser** — see below. |
| 11.2-verify | Browser-verify the address phone field | DONE | Verified in a real browser at 1280×900 and 375×812 against the **live API and live Atlas**, not a fixture. All five checks pass. (a) "Mobile number for delivery" renders on its own row between email and street, `inputMode="numeric"`. (b) Typed `98765abc4321012` → field holds `9876543210`: letters stripped, capped at 10. (c) Saving without it shows "Please enter a valid 10-digit mobile number for delivery." against the phone field with an error border — and a direct `curl` **without** the client in the way returns the identical string from the server, so the two really do agree rather than merely looking alike. (d) A pre-11.2 address was reproduced by `$unset`-ing `addresses.$[].phone` directly in Atlas; the API then omits the key entirely (proving `omitempty` rather than `""`), the edit form repopulates every field with phone **empty** (`existing.phone ?? ''`), and Update is refused until it is filled. Filling it round-trips and persists. (e) See 11.2-touch below — **not** a pass, and not a regression. No console errors. **Sign-in cannot be driven from a harness** (MSG91 widget + real OTP, no dev bypass), so the session came from a bearer token minted by a throwaway `cmd/devtoken` built against the real issuer and **deleted immediately after each run** — `cmd/` is back to adminpass/api/seed. Test data created and then cleaned up: the address and cart were deleted via the API. **`users` still holds a test user with phone `9000000001`** — see 7.11. |
| 11.7b | Wire 11.4 + 11.5 into the console — order detail and the advance control | DONE | The API had run ahead of the UI: the list screen had no row-click and no advance control because neither endpoint existed when it was built. `lib/api/orders.ts` gains `getOrder`/`setFulfilment`; order ids in the list link through; new `app/orders/[orderId]/page.tsx` shows fulfilment, delivery contact, items, totals and payment, with a **single next-step button**. `NEXT_STEP` mirrors the server's sequence only to *label* the button — the comment says plainly it is a label, never a permission; the server re-checks and its 409 message is shown verbatim rather than one invented client-side. **A 409 also triggers a re-read**, because a refusal usually means somebody else moved the order and leaving a stale badge on screen is worse than the error. **Verified in a browser against live Atlas**: clicked none→Processed→Transit on `EFF-246030` with the button relabelling each time; then advanced it to shipped by curl *behind the page's back* and clicked the now-stale button — got "This order is already Shipped.", the chip corrected itself to Shipped and the button disappeared, which is the concurrency guard working end to end. Money checks out (3×₹200 + 1×₹450 = ₹1,050, matching the list). 404 renders "No such order" without signing the operator out. **One layout defect found and fixed**: I reused `.card`, which carries `max-width: 400px` for the login form, squeezing every panel into a 400px column inside an 1100px page. Split into `.detail-panel` rather than widening `.card`, which would have changed the login screen. Console clean — the six logged errors are all deliberate probes (422/401/409/404) plus an HMR socket blip. `npx tsc --noEmit` clean, `npm run build` succeeds with both routes. **Test data: `EFF-246030` and `EFF-462617` in live Atlas now read `fulfilment: shipped`** — smoke-test orders, purge under 7.11. |
| 11.7c | Print / download the shipping label from the console | DONE | Owner request: an operator clicks and gets the label. **Generation was already on demand** — nothing is stored, the endpoint renders fresh from the order every time, which is why a label can never be stale against a changed address. What was missing was the UI entry point. `lib/api/orders.ts` gains `fetchLabelHTML`, which does **not** go through `request()` — that helper parses the JSON envelope and this endpoint returns a whole HTML document — but still decodes an error envelope so the server's own 409/503 message surfaces. **A plain link cannot work**: the endpoint needs a bearer token and `window.open` sends no Authorization header. Two controls: **Print label** writes the HTML into a hidden same-origin iframe and calls `print()` on it — a popup would be blocked, since the call is not synchronous with the click, and the iframe also lands the operator straight in the print dialog where they either print to the thermal printer or Save as PDF; **Download** saves `label-EFF-######.html` via a blob URL, revoked on a delay because revoking immediately cancels the download in some browsers. The panel is hidden entirely for a status the server would refuse, mirroring `paidStatuses` — the comment says plainly the server still enforces it and this only avoids offering a control that always errors. **Verified in a browser**: Download produced `label-EFF-816040.html` from a blob with no error; Print created a hidden (0-width) iframe containing the real label with the right order id and `size: 4in 6in`, calling `print()` exactly once; and an expired order shows no label panel and no advance button. `npx tsc --noEmit` clean, `npm run build` succeeds. **Note for future browser work: a real `print()` blocks the Browser pane on a native dialog** and the tab has to be replaced — stub `contentWindow.print` when testing this path. |
| 11.7-touch | Re-check `/orders` at a true 375px | TODO | 11.7's 375px check could not be run: the browser pane floors at `innerWidth` 508 regardless of what the resize call reports, so **treat "no overflow at 375px" as unverified** rather than passed. Everything structural was confirmed at 508 (table scrolls in its own wrapper, no control under 44px, no page overflow), so this is a confirmation pass, not an expected fix. Check on a real phone or a browser that honours the width. The same pane limit will block any future 375px claim — do not report one without checking `innerWidth` first. |
| 11.2-touch | Address form inputs are 36px tall on touch, not 44 | TODO | Found during 11.2-verify. At 375px with `pointer: coarse` genuinely matching (checked via `matchMedia`, not assumed), every address input measures **295×36**: name, email, phone and pin alike. **Not a regression from 11.2** — the new field matches its siblings exactly; 7.14's `@media (pointer: coarse)` block raised buttons, links and pills but never covered `.field input`, so this has been true since the form was built. The whole form should move in one pass rather than the phone field alone, which is why it is filed rather than fixed inline. No horizontal overflow at 375px. |
| 11.10 | Freeze `customerPhone` onto the order at creation | DONE | **Pulled ahead of 11.3** because the admin order object's `customer.phone` reads from this field — building the list endpoint first would have shipped a DTO sourcing a field that did not exist. `Order.CustomerPhone` (`bson:"customerPhone,omitempty"`), set in `OpenCheckout` via a pure `deliveryPhone(addr, accountPhone)`: the **address's own** phone wins (for a gift that is the recipient at the door, not the buyer), account number is the fallback for addresses predating 11.2. `auth.Service.AddressFor` now returns `(Address, accountPhone, error)` rather than gaining a second method — **it already loads the user document to find the address**, so the account number comes back for free instead of costing a second query. Only the order service called it, so no other caller moved. `Order.Validate` checks the phone **only when set**: orders written before the field are real purchases and must stay readable, but a malformed value is rejected — a label printed with a mangled number is worse than one printed with none. Tests: a two-row table pinning both branches of the fallback, each also asserting `ShippingAddress.Phone` is **unmodified** (the frozen contact is a separate field, not a rewrite of the snapshot), plus five `Validate` rows (absent, valid, too short, country code, letters). lint 0 · internal/order 98.0% · internal/auth 94.6% · total 96.2% · `make test` green (race). |
| 11.3 | `GET /api/v1/admin/orders` — status + fulfilment filters, `createdAt` cursor | DONE | New `internal/order/admin_repository.go` (`AdminFilter`, `ListAll`) and `admin_handler.go` (`AdminHandler`, the admin order DTO), mounted under `/admin` with `admin.Require` — in `internal/order` because it is an order surface, borrowing only the middleware, the same shape `catalogue.AdminHandler` uses. `server.Deps.AdminOrder` + one line in `main.go`. Index 7 `status_1_createdAt_-1` added to `EnsureIndexes` (not partial — it must serve `?status=all`). **`paidStatuses` extracted to one place** and now backs both `ListForUser` and the admin default, so the two lists cannot drift into showing an operator an order its buyer cannot see; `PaidStatuses()` hands out a **copy**, with a test that pins it. **`fulfilment=none` uses `$nin` over the stored values, not `$exists:false`** — an untouched order can be missing the key, null, or an empty string from some older path, and `$exists` catches only one of the three. Mixing `none` with named states becomes an `$or`. Cursor is `$lt`, never `$lte`, with a test that fails on `$lte`: the boundary row would otherwise be re-served as the first row of the next page. A cursor is offered **only on a full page**, so an operator is never invited to page into nothing. **Verified live against Atlas**, not just stubs: 401 without a token and 405 for DELETE (so it is routed and gated), 7 orders on the default vs 15 on `?status=all` — the default really does hide 8 abandoned checkouts — `fulfilment=none`→7, `packed`→0, and a full 5-page walk at `limit=3` returning **15 rows, 15 unique**, no duplicates and no skips. All seven 422 paths return the right message. lint 0 · internal/order 98.2% · total 96.3% · `make test` green (race). |
| 11.3b | Environment-split default on the order book | DONE | Owner decision (2026-08-17). Omitting `?status` returns the five paid statuses in `production` and **every** status everywhere else — a developer's question is usually about the attempt that failed, and needing to recall `?status=all` to see a declined payment is friction at the wrong moment. `AdminHandler` takes `config.Environment`; `defaultStatuses()` is the single home of the split, and `parseAdminFilter` takes the default as an argument so it stays a pure function of its inputs. **Only the default splits** — an explicit `?status=…` and `?status=all` mean the same thing everywhere, so a filter verified in dev cannot quietly return something else in production; a dedicated test pins that in both environments. An unset/unrecognised `APP_ENV` is treated as *not* production: failing closed would hide the failed payment from the developer debugging it, which is the whole point. **`GET /api/v1/orders` is untouched** — a shopper never sees a checkout attempt presented as a purchase, in any environment. Verified live with `APP_ENV=development`: default 15 (placed + expired), `?status=placed` still 7. lint 0 · internal/order 98.2% · total 96.3%. |
| 11.4 | `GET /api/v1/admin/orders/{orderId}` | DONE | `Repository.ByOrderIDUnscoped` + `getOrder`, reusing 11.3's `toAdminOrderDTO` so list and detail cannot describe the same order differently. **Named "unscoped" on purpose**: `ByOrderID` takes a userID and that filter is the only thing stopping one shopper reading another's order, so a call site that grabs the wrong one should read wrongly at a glance. A dedicated test asserts the filter carries `orderId` and **no `userId`** — with a stray scope the admin book would 404 on every order, since an admin has no user document. A malformed id is a **404 without a query**: it cannot name a real order, and unlike the shopper's endpoint there is no ownership dimension to leak, only existence. Tests: 200 with the object, 404 across four shapes (unknown id, garbage, wrong digit count, lowercase prefix) each asserting whether the DB was touched, 401 with the store untouched, 500 with the driver error hidden, plus repository not-found and driver-failure paths. **Verified live**: `EFF-462617` returns 2 lines / 65000 paise / `Not started`; `EFF-000001`, `nonsense`, `EFF-42` and `eff-462617` all 404; no token 401. lint 0 · internal/order 98.2% · total 96.3% · `make test` green (race). |
| 11.5 | `PATCH /api/v1/admin/orders/{orderId}/fulfilment` | DONE | `Repository.SetFulfilment` + `setFulfilment` handler. **The write is guarded, not read-then-write**: the filter carries the fulfilment state the server just read, plus `status: "placed"`. Without that guard two operators clicking at once would both succeed and skip a step — a parcel going packed→shipped with nobody marking it in transit — and an order that expired between the read and the write could still acquire fulfilment progress. A guard miss is a 409 ("Somebody else changed this order just now"), never a false 200. `$set` writes **exactly** `fulfilment` and `updatedAt`; a test counts the fields so a future edit cannot smuggle in a third. **409 messages name what the operator can do**, via a new exported `NextFulfilment` — "This order is Not started. The next step is Processed, not Shipped." A bare refusal leaves them guessing which button was right. `TestNextFulfilmentAgreesWithCanFulfil` pins the two against each other so a message can never name a step the guard would refuse. **422 vs 409 is a deliberate split**: 422 = "there is no such state" (including `none`, which an order can be *in* but never move *to*), 409 = "you cannot go there from here". **Verified live on real Atlas data**: `EFF-462617` walked none→packed→in_transit→shipped, all 200; then backwards, self-move, skip-a-step, an expired order, an unknown value, `none`, a missing field, an unknown field and a bad id all returned the right code and message. Afterwards its `status` was still `placed` and `totals.total` still 65000 — **only fulfilment moved** — and `?fulfilment=shipped` now finds it. **Note: `EFF-462617` in live Atlas now has `fulfilment: "shipped"`.** It is smoke-test data due for purge under 7.11; no real order was touched. lint 0 · internal/order 97.7% · total 96.2% · `make test` green (race). |
| 11.11 | **Every placed order has an empty `payment.method`** — root cause found and fixed | DONE (fix), VERIFY ON DEPLOY | **Confirmed a live defect, not stale data**: an order placed by the owner via netbanking on 2026-08-17 recorded `method: null`. **Root cause**: Razorpay's browser callback returns only an order id, a payment id and a signature — it genuinely cannot know how the shopper paid, so `service.go` never set the method. Only the webhook carries it. But the callback is synchronous with the browser and almost always wins the race to `MarkPlaced`, whose filter demands `status: "pending_payment"`; the webhook's richer update then matched nothing and its method was **discarded**. This would have happened in production too — the missing tunnel only hid it more completely. Nothing caught it because `MarkPlaced` does not re-validate, and `Order.Validate` would have rejected a placed order with no method. **Fix**: `Repository.FillPaymentDetail`, a narrow second write the webhook makes when `MarkPlaced` reports not-modified. Guarded on `status: "placed"` **and** the method still being empty, so it is idempotent, cannot resurrect an expired order, and cannot overwrite a correctly-recorded method. Writes only the detail sub-fields and `updatedAt` — a test enumerates `$set` and fails if it ever touches `status`, `placedAt`, the amount, or the callback's stored signature. A fill failure logs but still answers 200, since a non-2xx would make Razorpay redeliver forever over a detail field. **Cannot be verified locally**: Razorpay rejects tunnel hostnames, so no webhook has ever reached this machine and the method will stay null in dev. **Verify on the first deploy** — place one order and confirm the method is stamped. | Found during 11.3's live verification, and only visible against real data. All 7 placed orders return `payment: {method: null, label: null, …}` while carrying a real `razorpayOrderId` and `razorpayPaymentId` — so money moved but the method was never stamped. `Order.Validate` requires a method on a placed order, which means these documents would fail their own model's rules; `MarkPlaced` does not re-validate, so nothing caught it. Either the callback/webhook is not writing the method, or these predate that code. **Decide before launch**: a receipt that cannot say how it was paid is a support call. They are smoke-test orders due for purge under 7.11, so this may be historical — confirm by placing one real test order end to end and checking the stored payment sub-document. |
| 11.9 | Remove the dead fulfilment values from `Status` | TODO | `packed`, `shipped` and `delivered` are `Status` values that nothing produces now that fulfilment is its own field, and two fields both containing "shipped" is how a wrong branch gets written later. `cancelled` is order-level rather than fulfilment and is a separate question. Nothing in Atlas holds any of them, so removal is safe — but it is a contract change (`roadmap.md` status list, `schema.md` lifecycle, `ListForUser`'s `$in`, `Order.Validate`'s placed-and-later arm, the storefront's `STATUS_LABEL`), so it gets its own unit rather than riding along. |
| 11.6 | `GET /api/v1/admin/orders/{orderId}/label` — 4×6 HTML label | DONE | New `internal/order/label.go` (`html/template`, stdlib — `go.mod` still at four direct dependencies) + `getLabel`. `config.ShipFromAddress` with the six `SHIP_FROM_*` vars, **validated at startup and all-or-nothing**: some-but-not-all fails the boot naming each missing variable, and a non-6-digit PIN or non-10-digit phone fails too — a malformed PIN is worse than a missing one because it prints and the parcel still goes nowhere. None set is not an error; the endpoint 503s. New `httpx.CodeUnavailable` (503) rather than reusing `INTERNAL`, so an operator can tell "nobody set this up" from "something crashed". `RenderLabel` builds a **flat view** rather than passing the Order, so the template cannot reach a field that should never be on a parcel; a test asserts no razorpay id, no money and no email appear. `html/template` escaping is tested against a `<script>` in a shopper-supplied name. The recipient phone is the **frozen `customerPhone`**, not the address's — for a gift they differ — and the row is omitted entirely when absent rather than printing an empty bold line. Label refused (409) for any unpaid status, via `isPaid` sharing `paidStatuses` so "labelable" and "real" cannot drift. **Verified live**: real label for `EFF-816040` at exactly 384×576px = 4×6in, `Cache-Control: no-store`, with the return address, PIN, pickup phone, contents and the PREPAID strip; 409 expired, 404 unknown/malformed, 401 no token. **Typography raised after measuring, not guessing**: content ended at 50% of the label, so the recipient block — what a courier reads at arm's length — went 15→19pt with the address 12→14.5pt; re-measured against the worst realistic case (32-char name, two-line street, phone, six lines) at **86%, no clipping**. lint 0 · internal/order 96.9% · config 100% · total 96.0% · `make test` green (race). | `html/template` (stdlib — keeps `go.mod` at four direct dependencies; a PDF library would be the first new one since the project started). `@page { size: 4in 6in; margin: 0 }`, monochrome, no filled panels (a thermal head prints by burning; solid areas smear and shorten its life). 409 for an unpaid order. 503 when the origin is unconfigured — never a label with blanks where the return address belongs. |
| 11.7 | Order book list screen in `enerzia-admin/` | DONE | Built by a subagent from a dispatch prompt; **I verified it myself in a real browser against the live API and live Atlas**, which the subagent could not do (no plaintext admin password — it signed in with a token minted from `JWT_SECRET` on my side). `lib/api/orders.ts` + order types, `/orders` screen, nav entry in `PageShell`, styles. **All eight previously-unverified checks now pass:** 15 rows matching the API exactly (`EFF-462617`, 65000 paise → `₹650`, 2 items); `?status=placed` → 7 rows all Placed; `?fulfilment=packed` → 0 rows with the **filter-specific** empty state, distinct from "no orders"; cursor paging at limit 10 gives 10 then 5 = 15 with **no overlap**, Next disabled on the short page, Prev disabled on page 1, and Prev returns page 1 **byte-identical**; a 422 returns its message through the browser (CORS covers error responses); a bogus token bounces to `/login` **and clears sessionStorage**; no console errors (the three present were my own probes). **One defect found and fixed:** the fulfilment filter offered "In transit" where `Fulfilment.Label()` returns "Transit" — an operator would filter by one word and read another in the rows returned, exactly the second-vocabulary drift 11.8 exists to undo. Fixed with a comment naming the rule. **One check could not be run:** 375px. The browser pane floors at `innerWidth` 508 and would not go narrower, so the 375px claim is **unverified** — at 508 there is no page overflow, the table scrolls inside its own `overflow-x:auto` wrapper rather than the body, and nothing measures under 44px with `pointer: coarse` genuinely matching. **A near-miss worth recording:** the order id renders in a bold face where a hyphen looks like an en dash; a codepoint check showed U+002D, so it was a reading error, not the 9.2 truncation bug repeating. `npx tsc --noEmit` clean after the fix. |
| 11.8 | Map fulfilment into the shopper's account area | TODO | `Enerzia/components/shop/OrdersScreen.tsx`. **Deferred by design, not by omission** (owner, 2026-08-17): the split exists so a shopper's view can be designed on its own terms instead of being driven by an operator's clicks. Nothing shopper-facing returns `fulfilment` today, so this task starts with a contract decision — does `GET /api/v1/orders` expose it, and as what words? Until then an order reads "Confirmed" to its buyer from payment through dispatch. **Checked during 11.1, no live risk:** the storefront's `STATUS_LABEL` falls through to `?? s` for anything it does not know and `statusClass` returns the muted chip, so an unmapped value degrades rather than crashes. |

### Credentials needed

| what | why | status |
|---|---|---|
| `SHIP_FROM_*` (name, line1, city, state, pin, phone) | the return address printed on every label | **waiting** — owner puts them straight into `.env`; `.env.example` carries the empty keys. 11.6 ships against an unconfigured origin (503) so nothing is blocked. |

---

## Discovered along the way

Things found mid-task that are not part of the task in hand.

| found in | item |
|---|---|
| 7.16 | **`npm run lint` in `Enerzia/` is broken and has been for a while.** The script is `next lint`, which Next 16 removed; it now fails with "Invalid project directory provided, no such directory: …/lint". So the storefront has had no lint gate at all — only `npm run typecheck`. Pre-existing, unrelated to 7.16, and left alone deliberately rather than fixed mid-task. Fix by migrating to `eslint .` per Next's codemod (`npx @next/codemod@canary next-lint-to-eslint-cli`), and consider adding it to CI alongside the Go gate. |
| 7.16 | **An `onError` fallback on a server-rendered `<img>` is not enough on its own.** The image ships in the SSR HTML and the browser starts fetching immediately, so a 404 that resolves before hydration fires `error` with no handler attached and the fallback silently never runs. Needs a mount-time `el.complete && el.naturalWidth === 0` check as well. The PDP's 9.5 fallback has the same shape and may have the same hole — worth a look when that code is next touched. |
| 7.16 | **A media query adds no specificity.** A `@media (max-width: …)` block placed earlier in the file than the rule it means to override loses to it. Cost one round on the header gap fix; the block now sits after `.site-nav` with a comment saying why. |
| 10.2 | dummyHash must match the configured hash's bcrypt cost, not a fixed MinCost. MinCost is ~1ms; DefaultCost is ~60ms — mismatched costs make email enumeration trivial via timing. Cost is read with `bcrypt.Cost([]byte(cfg.PasswordHash))` at service construction; a malformed hash (bad paste) zeroes out the fields and makes the service return `ErrNotConfigured`. |
| 10.2 | Limiter must be checked before the "not configured" guard in `Login`. If the guard runs first, a configured and unconfigured service answer 429 at different rates (configured: limiter fires after 10 failures; unconfigured: never), which is a reliable oracle for "is an admin account set up?" |
| 10.2 | `PathPrefix("/admin/me").Subrouter()` with `HandleFunc("")` also matches `/admin/mexyz` and falls through to gorilla's plain-text 404, bypassing the envelope. Use one `/admin` subrouter with `HandleFunc("/me", ...)` instead. |
| 10.2 | `http.Error(w, rawString, 401)` sends `text/plain`, not the JSON envelope all other errors use. Use `httpx.WriteError` with `httpx.CodeUnauthorized`. |
| 10.1 | `validateDetail` (in `content.go`) is where image validation landed, alongside the existing badge/nutrition checks — consistent with the error-format style used there. |
| 0.1 | Frontend holds money as whole rupees; API uses paise. Conversion needed at the boundary — tracked as 7.1. |
| 0.1 | Frontend OTP accepts any 6 digits (`product.md` §3.3). Real check lands in 2.3. |
| 0.6 | `mongodb.Disconnect` was not idempotent — a second call returned `client is disconnected`. The fake caught it; now treated as success, since a deferred close plus an explicit one is a normal shutdown shape. Fixed in place. |
| 0.6 | golangci-lint hides all but 3 occurrences of an issue by default; `max-same-issues: 0` set so systemic problems are not silently truncated. |
| 1.1 | Slugging is lossy, so a `lineId` cannot be reversed into a variant label. Resolution must go product → `VariantBySlug`, never by un-slugging. `Product.Validate` rejects two labels that slug alike so ids stay unique. |
| 1.2 | Schema had never been written down. `schema.md` now documents all six collections, their indexes and four modelling decisions (cart stores no prices; orders snapshot everything; product `_id` is a readable string; one cart/address doc per user). |
| 3.4 | Cart writes are read-modify-write; one shopper on two devices could lose an add. **Closed by the user — not an MVP concern.** |
| M.5 | Splitting sizes into independent products **removed** a whole collection and the id-mapping layer with it. Worth noting: the earlier variants split was the right call *for embedded variants*, but once each size is a product the same atomicity comes free. |
| M.5 | Sibling cross-links (`GET /products/{id}` → `siblings`) exist so the relationship between sizes is not lost when they become independent. Nothing about buying depends on `family`. |
| M.1 | Orders keep duplicating name, label and price. Raised as "repetition" but it is a deliberate snapshot: referencing a `variantId` alone would let tomorrow's price change rewrite last week's receipt. `variantId` is kept alongside for traceability only. |
| M.3 | Addresses stayed **embedded** rather than becoming their own collection: only ever read with their owner, and a handful per user. A collection would add a join to every checkout for nothing. Variants were different — stock needs atomic per-variant writes. |
| M.5 | Migrating live Atlas needed a manual drop of the `variants` collection and all old product documents — `Seed` upserts but never removes superseded shapes. |
| M.2 | Live Atlas carried a stale `products.form_1` index from the old schema alongside the new `form_1_position_1`. Dropped. `EnsureIndexes` creates but never removes, so superseded indexes need a manual drop. |
| M.2 | Re-seeding must not reset live inventory: `stock` is under `$setOnInsert`, and variants are matched on `(productId, label)` so their generated ids survive a re-seed. Pinned by a test. |
| 3.2 | Shipping must be decided from the **subtotal**, never the MRP total — otherwise a heavily discounted cart earns free delivery it has not paid for. Pinned by a test. |
| 3.1 | A cart line whose variant has left the catalogue is **dropped** on read rather than priced at zero. A vanished line is visible to the shopper; a ₹0 charge would not be. |
| 2.1 | **Corrected my own `schema.md` decision.** It said unsalted SHA-256 was fine for OTP codes because they are short-lived. That was wrong: the keyspace is 10^6, so anyone able to read `otp_codes` could reverse every live code instantly and sign in as anybody. Now HMAC-SHA256 keyed with a server-side pepper and domain-separated by phone number. `schema.md` updated. |
| 2.4 | `gosec` G101 fires on any constant whose name contains "token"; renamed `tokenIssuer` → `issuerName` rather than adding a suppression for a false positive. |
| 2.3 | `govet` shadow check caught an inner `err` shadowing the outer one in `Verify` — harmless today, but exactly the shape that swallows an error later. |
| 1.4 | **gorilla/mux loses the method-mismatch signal**: a later route failing on its *path* matcher clears it, so only the last-registered route ever answers 405. `POST /api/v1/products` returned 404. Fixed generally in `server.methodAware`, which re-matches the path under the other verbs. Reproduced in isolation first, so it is a mux behaviour and not our wiring. |
| 1.4 | A subrouter inherits neither `NotFoundHandler` nor `MethodNotAllowedHandler` from its parent; both are now set on `/api/v1` explicitly. |
| 1.3 | The driver sends bulk write payloads as OP_MSG **document sequences**, not inside the command body — `mongotest` now parses section type 1 so write assertions are possible at all. |
| 1.1 | `ParseFilter` treats form matching as case-sensitive (`powder` is rejected, `Powder` accepted), since the roadmap types the values as exact literals. Loosen it in 1.4 if the frontend ever sends lowercase. |
| 5.1 | The dispatch prompt named the snapshot line type `OrderLine`, but repo lint (revive) forbids the `order.OrderLine` stutter and cart already uses `cart.Line`. Named it `order.Line` to match — a naming choice, not a contract change. |
| 5.x | **Razorpay decision, 2026-08-07.** Payment moves to Razorpay Checkout; COD dropped. Contracts amended first, in order: `schema.md` (decision 6, `orders` rework, `payment_events`, §Razorpay), `roadmap.md` (§Orders rewritten, `GATEWAY_ERROR` added), `product.md` (§3.5 rewritten). Phase 5 grew from 4 tasks to 9. |
| 5.x | The frontend's checkout screen still renders its own UPI/card fields and a COD option (`components/shop/CheckoutScreen.tsx`, `lib/shop/data.ts` `PAY_OPTIONS`). All of it is dead under Razorpay — folded into task 7.1, which already owns the 4-cards→9-cards rework. |
| 5.1 | ~~`payment.last4` null-vs-empty~~ — **resolved by 5.2.** The whole `Payment` struct was replaced; method-detail fields are now absent until Razorpay reports them, and the empty→`null` mapping happens at the JSON edge. |
| 6.2 | `Vary: Origin` is set only when an `Origin` header is present, so a response to a **no-Origin** request carries no `Vary` at all. Strictly it should: the same URL does return different headers depending on `Origin`. Accepted deliberately — the unsafe direction is protected (an allowed-origin response always carries `Vary`, so a cache cannot serve it to a different origin), the only failure mode is a shared cache causing a *false block*, and there is no shared cache in front of this API. One-line fix (hoist the `Vary` set above the empty-origin return) if a CDN is ever put in front. |
| 5.8 | `FindExpiredPending`'s `cursor.All` decode-error branch is uncovered (that function sits at 77.8%). Reaching it needs a cursor that succeeds on `Find` but fails **during** document streaming, which `mongotest` cannot currently produce. Accepted: `internal/order` is 98.1% overall. Revisit if `mongotest` ever grows a streaming-error capability — the same branch in `ListForUser` would benefit. |
| 5.4 | **Testing a duplicate key needs `mongotest.Reply`, not `Fail`.** `Fail` sends `ok:0`, which the driver turns into a `CommandError` carrying no `WriteErrors` — so a dup-key test written with it silently exercises the wrong path and proves nothing. A real E11000 arrives as `ok:1` with a `writeErrors` array, which is what makes `WriteException.WriteErrors[0].Raw` (and its `keyPattern`) available at all. Applies to any future write-error test. |
| 5.2 | `.env.example` still documents `MONGO_TEST_URI`, which `internal/mongotest` made unnecessary (see the 0.6 rows). Harmless but misleading to a newcomer — clean up whenever `.env.example` is next touched. **Resolved in 5.3: `MONGO_TEST_URI` comment now clarifies the var is optional and tests skip without it; the key is retained so the mechanism is visible.** |

---

## Needed from the user

| what | why | status |
|---|---|---|
| ~~MongoDB Atlas SRV URI~~ | supplied and working; catalogue seeded, indexes created | resolved |
| ~~Database name~~ | `enerzia` | resolved |
| ~~Atlas IP allowlist~~ | confirmed working from this machine | resolved |
| ~~A separate test database~~ | ~~`MONGO_TEST_URI`~~ — no longer required; `internal/mongotest` fakes the server | resolved |
| JWT signing secret | can be generated locally — no need to source one | self-serve |
| OTP delivery provider (WhatsApp or SMS) | **decision open** — neither is free; see handoff.md. `LogSender` covers development, `UnconfiguredSender` fails loudly in production | **waiting** |
| **Razorpay `KEY_ID` + `KEY_SECRET`** (test mode) | needed to create a Razorpay order at all; test mode moves no real money | **waiting** — 5.3 ships against a fake until they land |
| **Razorpay `WEBHOOK_SECRET`** | a **different** secret from KEY_SECRET, set when the webhook is added in the Razorpay dashboard. Verifying one with the other fails in a way that looks like a forged request | **waiting** |
| **Cloudinary `CLOUD_NAME`, `API_KEY`, `API_SECRET`** | needed before an image can actually be uploaded. Free tier is sufficient. 10.4 ships against `Unconfigured` until they land, so nothing is blocked on them | **waiting** |
| **`ADMIN_EMAIL` + `ADMIN_PASSWORD_HASH`** | the console cannot be signed into without them. **Generate the hash yourself** with `make admin-password` and paste it into `.env` — the password must not be typed into a chat, and nobody but you needs to know it | **self-serve** |
| A publicly reachable URL for the webhook | Razorpay must be able to POST to `/webhooks/razorpay`; a tunnel is fine for development | **waiting** — only blocks the live smoke test, not 5.7 |
